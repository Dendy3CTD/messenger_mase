# 🗺️ mase — Дорожная карта до версии 1.0

|                   |                                                                                                                                              |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| **Версия сейчас** | 0.3.0 (`versionCode = 3`, `android-client/app/build.gradle.kts:15-16`)                                                                       |
| **Цель**          | 1.0.0 — **рабочий инструмент проектной группы, замена Telegram**: «скачай APK по ссылке, зарегистрируйся, работай». Доступность **≥ 99,5 % в месяц** (жёсткое требование), работает из России без VPN. Бесплатный хостинг — желательное, но не обязательное условие |
| **Сервер**        | Go 1.23 + gorilla/websocket + SQLite (mattn/go-sqlite3, CGO) — `go-server/`                                                                  |
| **Клиент**        | Android, Kotlin + Jetpack Compose, Room v3, OkHttp WebSocket, minSdk 24 / targetSdk 34 — `android-client/`                                   |
| **Прод сейчас**   | `wss://mase.nemilk.ru/ws` → Cloudflare Tunnel `mase-server` → `127.0.0.1:8080` на домашнем ПК (`go-server/deploy/mase-tunnel.yml`, `run.sh`) |
| **Legacy**        | `cpp-server/`, `mase-server-gui.py`, `server_manager.py`, `build_apk.sh server` (LAN-версия v0.2, не используется)                           |
| **Git**           | под git с 2026-09-19 (первый коммит `eac0cf9`), ветка `main` |
| **Репозиторий**   | `git@github.com:Dendy3CTD/messenger_mase.git` (github.com/Dendy3CTD/messenger_mase), remote `origin`; push выполняет владелец |
| **Вехи**          | **0.9 «рабочая группа»** (после Ф8, раздел «Веха 0.9») → **1.0** (после Ф17) |
| **Срок**          | ориентир **6–10 месяцев** до 1.0 при ~10 ч/нед, до 0.9 — 3,5–5,5 месяцев (раздел 6.1; это оценка, а не обязательство) |
| **Обновлено**     | 2026-09-19                                                                                                                                   |

**Легенда статусов:** 🟢 работает · 🟡 частично · ⚪ отсутствует · 🔴 сломано или опасно (подтверждено чтением кода)

**Легенда объёма:** `S` — до 2 вечеров · `M` — 1–2 недели · `L` — 3–4 недели (ориентир при ~10 ч/нед; это оценка, а не обязательство)

> Все утверждения об обнаруженных проблемах получены **чтением кода**, приложение и сервер я не запускал. Такие пункты помечены «по коду». В фазах 5–8 первая задача — воспроизвести проблему тестом, прежде чем чинить.

---

## 0. Главное, что изменилось после аудита

Четыре вывода меняют порядок работ по сравнению с прежней картой в Obsidian:

1. **Требование «без VPN» под угрозой из-за хостинга, а не из-за кода.** С июня 2025 российские операторы режут соединения к Cloudflare до ~16 КБ (см. раздел 4). Текущий прод идёт через Cloudflare Tunnel, поэтому его надо **измерить с российских мобильных сетей до любой другой работы** (Фаза 1). От результата зависит выбор хостинга.
2. **Клиент и сервер расходятся в контракте, и «реализованное» часто не работает.** Фото и аватарки, индикатор набора, онлайн-статус, галочки прочтения и реконнект сломаны по коду (раздел 2.3). Групповые чаты, правка и удаление имеют серверную часть, но **нет ни одного экрана**.
3. **В сервере есть дыры в авторизации, которых нет в старой карте:** редактирование, удаление, «печатает…», квитанции и выход из группы работают без проверки, что пользователь состоит в чате. Тексты правок уходят всем онлайн-пользователям (раздел 3).
4. **Цель уточнена: mase — рабочий инструмент проектной группы, а не мессенджер «для друзей».** Доступность стала жёстким требованием (99,5 %), бесплатность — желательной. Отсюда: по умолчанию рекомендуется платный российский VPS с домашним ПК в резерве (раздел 4.6), введена веха **0.9 «рабочая группа»** сразу после базовых групп, фото и надёжной доставки, а поиск по истории, файлы до 50 МБ и закрепление сообщений — обязательные функции 1.0.

---

## 1. Нефункциональные требования (целевые для 1.0)

| Параметр | Цель | Обоснование |
|---|---|---|
| Назначение | Рабочий инструмент проектной группы (учёба и работа), замена Telegram — не «мессенджер для друзей» | сбой мессенджера останавливает работу группы, поэтому требования ниже жёсткие |
| Пользователи | до 50 зарегистрированных, до 25 одновременно онлайн; **до 10 человек одновременно работают в одном чате** | при росте свыше 100 — Postgres (см. «Идеи после 1.0») |
| Нагрузка | до 5 сообщений/с устойчиво, пик до 20/с; в чате на 10 человек рассылка одного сообщения всем ≤ 1 с | один процесс, одно SQLite-соединение (`db.go:21`) это выдерживает; проверяется нагрузочным тестом (Ф7) |
| **Доступность** | **≥ 99,5 % в месяц** (≈ 3,6 ч простоя за 30 дней); **единичный сбой не дольше 2 ч в рабочее время** (по умолчанию 08:00–20:00 МСК, Пн–Сб — уточнить, D8); автоперезапуск процесса ≤ 10 с; плановые работы — только вне рабочего времени | **жёсткое требование** (было 99 % и сбой до 24 ч); измеряется внешним монитором, отчёт раз в месяц (Ф4) |
| RPO (потеря данных) | ≤ 24 ч (ежедневный бэкап), цель — ≤ 1 ч если Litestream окажется надёжным | сейчас бэкапов нет вообще |
| RTO (время восстановления) | ≤ 2 ч с нуля на любой машине из копии, проверено учебным восстановлением; **≤ 30 мин** переключением на готовый резерв | совпадает с допустимым единичным сбоем; см. Ф2, Ф4 |
| Хранение текста | бессрочно, пока пользователь не удалил сообщение | |
| Файлы и медиа | обмен рабочими файлами **до 50 МБ**; квота **1 ГБ на пользователя** (было 300 МБ — мало для рабочих документов); фото после сжатия ≤ 5 МБ, голосовое ≤ 5 мин; «осиротевшие» загрузки удаляются через 1 ч; список всех файлов чата | значения конфигурируются; решение D6 |
| Поиск по истории | **обязательная функция**: по сообщениям и чатам, только в чатах, доступных пользователю; ≤ 1 с на 100 000 сообщений; локально (Room) и на сервере (SQLite FTS5) | для проектной группы — способ находить решения и договорённости (Ф12) |
| Удаление аккаунта | Аккаунт помечается удалённым (`deleted_at`), сессии отзываются; **сообщения остаются**; профиль показывается как «Удалённый пользователь» (без аватара и био); логин не переиспользуется; из групп пользователь выводится системным сообщением; **медиа удаляются через 30 дней** (срок — параметр) | политика; сервер — Ф6, интерфейс — Ф8 |
| Блокировка и бан | Пользователь блокирует другого: тот не может писать в личку и не видит его онлайн-статус; администратор сервера банит аккаунт командой `mase ban` (на случай утечки инвайт-кода) | Ф6 |
| Хранение логов | 14 дней в journald; без паролей, токенов и номеров телефонов | |
| Целевой размер APK | **≤ 15 МБ** релизный (сейчас debug ≈ 17,4 МБ, `dist/*.apk`; R8 выключен, `build.gradle.kts:25`) | |
| Совместимость | Android 7.0+ (`minSdk = 24`) | |
| Задержка доставки | онлайн-получателю ≤ 1 с в 95 % случаев | |

---

## 2. Где мы сейчас (результат аудита)

### 2.1 Сводка по областям

| Область | Состояние | Что конкретно отсутствует / не так |
|---|---|---|
| Пароли | 🔴 | Голый SHA-256 без соли (`internal/auth/auth.go:13-16`), сравнение не в постоянном времени (`handler.go:146`); комментарий «Matches the C++ server» (`auth.go:11-12`) — унаследовано из legacy |
| Токены и сессии | 🔴 | Нет срока жизни (`db.go:38-42`, проверка `db.go:194-198` не смотрит `created_at`); `DeleteToken` определён (`db.go:200`), но **нигде не вызывается**; нет `auth.logout`, смены пароля, отзыва; токен хранится в БД открытым текстом, отправляется в каждом кадре и в `?token=` (`media.go:129-136`) |
| Проверка прав на действия | 🔴 | См. раздел 3, пункты R-11…R-15 |
| Регистрация | 🟡 | Телефон не подтверждается (нет SMS), проверка только `len ≥ 4` (`handler.go:163`); нет верхних границ длины; открытая регистрация без инвайтов |
| Личные чаты (текст) | 🟡 | Работают (`handler.go:370-407`), но: нет `clientMsgId`/ACK/дедупликации; не проверяется, что собеседник существует (`EnsureDirectChat` создаёт чат с любым id); `replyToId` не проверяется на принадлежность чату |
| Общий чат | 🟡 | Все зарегистрированные автоматически участники (`db.go:231-233`), рассылка всем онлайн (`handler.go:351-366`); нет лимитов, пустое тело молча игнорируется, push нет |
| Групповые чаты — сервер | 🟡 | Создание, отправка, фото, список, информация есть (`handler.go:573-778`). Нет: переименования, удаления участника, смены роли, передачи прав. Роли `admin/member` хранятся (`db.go:75-88`), но **не проверяются нигде**; `group.leave` не проверяет членство (`handler.go:710-727`); `chat.history` для группы даёт `forbidden` (`handler.go:468-474`, `db.go:231-243` знает только direct); в истории нет `senderName` (`handler.go:482`) |
| Групповые чаты — клиент | 🔴 | Методы в `MessengerEngine.kt:521-564` **никто не вызывает** (проверено поиском по `ui/`); нет экранов создания и информации; `AppRoot.kt:71-78` открывает `ChatScreen` без `isGroup`, то есть группа откроется как личный чат без `peerUserId` |
| Редактирование / удаление — сервер | 🟡 | `handler.go:509-541`, `db.go:315-325`; удалённое сообщение в истории приходит пустым, флага `isDeleted` в JSON нет (`buildMsgJSON`, `handler.go:837-858`) |
| Редактирование / удаление — клиент | 🔴 | Нет обработчиков `evt.message.edited/deleted` в `MessengerEngine.kt:194-420`, нет UI; в Room нет полей `editedAt`, `isDeleted`, `replyToId` (`MaseDatabase.kt:29-40`) |
| Ответ на сообщение | 🟡 | Сервер хранит `reply_to_id` (`db.go:60`) и отдаёт; клиент не отправляет и не показывает |
| Пересылка | ⚪ | Отсутствует и в протоколе, и в схеме |
| Фото — отправка | 🟡 | Клиент сжимает до 1280 px / JPEG 85 (`MediaUploader.kt:19-20,101`), загрузка одним куском без прогресса и отмены (`MediaUploader.kt:63-83`) |
| Фото и аватарки — отображение | 🔴 | Клиент запрашивает `/media/{id}` **без токена** (`ChatScreen.kt:430`, `AvatarView.kt:38`, Coil без интерцептора), сервер требует токен (`media.go:110-114`) → 401. По коду; на устройстве не проверял |
| Файлы (документы) | ⚪ | Нет UI, нет `msgType=file` в отправке, нет имени/размера/mime в схеме |
| Голосовые | ⚪ | Колонка `duration_sec` и расширения `.ogg/.m4a` (`media.go:138-160`) есть, но нет ни записи, ни плеера, ни `RECORD_AUDIO` в манифесте, ни обработчика отправки |
| Push | ⚪ | `push.go` — заглушка, только лог, и там же паника при токене короче 8 символов (`push.go:17`); вызывается только для личного текста (`handler.go:398-406`); одна запись на пользователя (`db.go:70-74`), названа `fcm_token`; на клиенте нет ни Firebase, ни foreground-сервиса, ни `POST_NOTIFICATIONS` (`AndroidManifest.xml:4-9`) |
| Доставка и потери | 🔴 | Клиент шлёт «в никуда»: `WsMessengerClient.send` при отсутствии соединения только пишет в лог (`WsMessengerClient.kt:78-81`); очереди неотправленного нет; сообщение появляется в UI только после эха от сервера; Room использует серверный `id` как ключ (`MaseDatabase.kt:31`), непереданное сообщение хранить некуда; сервер при переполнении буфера получателя молча теряет событие (`hub.go:84-90`, результат `Send` не проверяется, `handler.go:396,456`) |
| Досинхронизация после разрыва | 🔴 | После подключения клиент запрашивает только `friends.list` и историю общего чата (`MessengerEngine.kt:180-185`). Личные и групповые сообщения за время офлайна **не подтягиваются**, пока чат не откроют; на новом устройстве список личных чатов вообще не восстановить (на сервере нет `chat.list`, `group.list` клиент не вызывает) |
| Реконнект | 🔴 | `runNetworkLoop` каждые 2→4→…→30 с открывает **новый сокет, не закрывая прежний**, и никогда не сбрасывает задержку (`MessengerEngine.kt:155-178`); сервер держит одно соединение на пользователя и выбрасывает старое (`hub.go:64-69`) → «мигающий» баннер «Нет соединения» |
| Пагинация истории | ⚪ | Жёстко последние 200 (`handler.go:475`), курсора нет |
| Статусы сообщений | 🔴 | Сервер ставит только `read` (`handler.go:500`), `delivered` нигде не выставляется; UI показывает `✓✓` только для `delivered` (`ChatScreen.kt:398`) → прочитанное отображается как `✓`. `handleReceipt` не проверяет получателя, содержит мусорный вызов `GetHistory(0,0)` (`handler.go:498`) |
| Онлайн-статус | 🔴 | Сервер шлёт массив чисел (`handler.go:815-823`), клиент разбирает как массив объектов (`MessengerEngine.kt:315-318`) → список всегда пуст |
| «Печатает…» | 🔴 | Клиент шлёт `isTyping: 1` числом (`MessengerEngine.kt:498`), сервер ждёт `bool` (`handler.go:547`) → ошибка разбора, событие молча отбрасывается |
| Профиль | 🟡 | Правка имени, `@username`, био и аватара есть (`SettingsScreen.kt:124`, `MessengerEngine.kt:455-464`), но без валидации пустого `username` и с невидимым аватаром (401); **экрана чужого профиля нет**; поиск человека по username есть (`ContactsScreen.kt:137`, `MessengerEngine.kt:571-575`), ведёт только к «добавить/написать» |
| Счётчик непрочитанных | 🟡 | Считается **на клиенте** (`incrementUnread` в `MessengerEngine.kt:274-299`, показ `ChatsListScreen.kt:122-152`), теряется при переустановке и на втором телефоне; на сервере счётчика нет |
| Даты, время, «был(а) в сети» | 🟡 | В списке чатов «HH:mm / d MMM» (`ChatsListScreen.kt:194-198`), в ленте только время (`ChatScreen.kt:347`), разделителей по дням нет; `last_seen` нет ни в БД, ни в протоколе |
| Тёмная тема | 🟡 | Переключатель есть (`SettingsScreen.kt:254`), по умолчанию выключена и не следует за системной (`SessionRepository.kt:39,86`); настройки размера шрифта нет |
| Блокировка, жалобы, бан | ⚪ | Отсутствуют (поиск по `go-server/` на `block`, `ban`, `report` пуст) |
| Удаление аккаунта | ⚪ | Отсутствует; поля `deleted_at` в схеме нет |
| Поиск по истории | ⚪ | Ни серверного FTS, ни локального (поиск по `fts`/`FTS` в репозитории пуст) |
| Закрепление чатов и сообщений, mute, архив, черновики, реакции, упоминания, ссылка-приглашение в группу | ⚪ | Отсутствуют и в протоколе, и в схеме |
| Схема БД и миграции | 🔴 | Только `CREATE TABLE IF NOT EXISTS` (`db.go:26-97`), версии схемы нет, `ALTER` невозможен; внешних ключей нет, `PRAGMA foreign_keys` не включён (`db.go:17`); `UNIQUE(username)` допускает пустую строку → второй пользователь с `''` получит `server_error` (`handler.go:257-265`). Room: `exportSchema=false` (`MaseDatabase.kt:75`), миграции 1→2→3 ручные |
| Обработка ошибок протокола | 🟡 | Клиент понимает только ошибки авторизации (`MessengerEngine.kt:408-418`); остальные коды теряются |
| Тесты | ⚪ | Ни одного `_test.go`, ни одного теста Kotlin (проверено поиском по репозиторию) |
| CI | ⚪ | Нет `.github/`, нет Dockerfile |
| Бэкапы | ⚪ | Скриптов нет; БД в режиме WAL лежит в `go-server/deploy/` рядом с бинарём |
| Логи и healthcheck | 🟡 | `log.Printf` без структуры, в лог пишутся номера телефонов (`handler.go:142,147,152,197`); `/health` всегда отвечает `ok` без проверки БД (`main.go:62-64`); алертов нет |
| Эксплуатация | 🔴 | `run.sh` запускает сервер и туннель фоновыми задачами оболочки (`run.sh:37,57`), без systemd; `mase.service` ссылается на `/opt/mase` и пользователя `ubuntu` (`mase.service:8-12`); `setup.sh` и `nginx.conf` описывают другой домен (`mase.duckdns.org`) и сертификат на `admin@example.com` (`setup.sh:46`) |
| Окружения | 🔴 | `run.sh` по умолчанию берёт БД `deploy/mase.sqlite` (`run.sh:10`), то есть **прод и разработка — один файл**; адрес сервера зашит в `BuildConfig` для всех сборок (`build.gradle.kts:18-19`), флейворов `dev/prod` нет |
| Сборка и подпись | 🔴 | В `release` нет `signingConfig`, `build_apk.sh:16` ждёт `app-release-unsigned.apk`; R8 выключен (`build.gradle.kts:25`), `proguard-rules.pro` — 1 строка; в `dist/` лежат 6 debug-APK (≈100 МБ); keystore отсутствует |
| Обновление приложения | ⚪ | Нет ни проверки версии, ни установки, ни `REQUEST_INSTALL_PACKAGES` |
| Резервные копии клиента | 🟡 | `allowBackup="true"` (`AndroidManifest.xml:13`), токен сессии лежит в обычном DataStore (`SessionRepository.kt:16,109`) |
| Документация | 🟡 | `README.md` описывает v0.2 (C++, LAN, OTP, порты 5555/5556); реальный протокол описан только в заметках Obsidian |
| Мёртвый код | 🟡 | `ServerDiscovery.kt`, `TcpMessengerClient.kt` на клиенте, `cpp-server/` и GUI-скрипты; клиент обрабатывает `friends.ok`, `profile.updated`, которые сервер не шлёт |

### 2.2 Протокол WebSocket: все типы сообщений и их использование

Обозначения: ✅ используется и совпадает · ⚠️ используется, но с ошибкой · ⛔ клиент не использует · ➖ не применимо.

**Клиент → сервер** (`handler.go:35-117`)

| type | Поля | Клиент |
|---|---|---|
| `auth.login` | phone, password | ✅ |
| `auth.register` | phone, password, displayName, username | ✅ |
| `auth.resume` | token | ⛔ (клиент кладёт `token` в каждый кадр, сервер авторизует по первому, `handler.go:47-61`) |
| `ping` | — | ⛔ (используются WS-ping кадры OkHttp, 30 с) |
| `profile.update` | displayName, username, bio | ✅ |
| `profile.set_avatar` | avatarMediaId | ✅ (но картинка не отобразится, см. 401) |
| `friends.add` / `friends.list` | friendUserId | ✅ |
| `user.lookup` | username / userId | ✅ |
| `chat.global.send` | body | ✅ |
| `chat.direct.send` | peerUserId, body, replyToId | ⚠️ `replyToId` не передаётся |
| `chat.direct.open` | peerUserId | ✅ |
| `chat.photo.send` | peerUserId, mediaId, replyToId | ✅ |
| `chat.history` | chatId | ⚠️ без курсора, для групп сервер отвечает `forbidden` |
| `chat.receipt` | messageId | ⚠️ см. статусы |
| `chat.message.delete` / `chat.message.edit` | messageId [, body] | ⛔ |
| `typing` | chatId, isTyping | ⚠️ тип поля не совпадает |
| `device.register_push` | fcmToken | ⛔ |
| `chat.group.create` / `send` / `photo` | title, memberIds / chatId, body / mediaId | ⛔ методы есть в движке, UI нет |
| `chat.group.open` | chatId | ⛔ |
| `group.add_member` / `leave` / `info` / `list` | chatId, userId | ⛔ (`info` запрашивается, ответ игнорируется, `MessengerEngine.kt:394-396`) |

**Сервер → клиент**

| type | Клиент |
|---|---|
| `pong` | ⛔ игнорируется |
| `auth.session` | ✅ (поле `phone` клиент ждёт, сервер не присылает — `userJSON`, `handler.go:827-835`, номер не отдаёт: это правильно) |
| `friends.list`, `chat.open`, `chat.history`, `user.lookup` | ✅ |
| `evt.message` | ✅ (для истории групп `senderName` не приходит) |
| `evt.receipt` | ⚠️ |
| `evt.presence` | ⚠️ разбор не совпадает |
| `evt.typing` | ✅ разбор, но событие не доходит |
| `evt.profile` | ⚠️ клиент лишь перезапрашивает друзей, свой профиль не обновляет |
| `evt.message.edited` / `evt.message.deleted` | ⛔ |
| `chat.group.created` / `chat.group.opened` | ✅ (создаёт строку в Room) |
| `group.info`, `group.member_added`, `group.member_left`, `group.list` | ⛔ |
| `err` | ⚠️ только коды авторизации |

Клиент ждёт, но сервер **никогда не шлёт**: `friends.ok`, `profile.updated`.

### 2.3 Схема БД сейчас

Таблицы (`db.go:26-97`): `users`, `tokens`, `chats(kind, u1, u2)`, `messages(chat_id, sender_id, body, msg_type, media_id, status, ts, reply_to_id, edited_at, is_deleted, duration_sec)`, `friends`, `push_tokens(user_id PK)`, `groups`, `group_members(role)`. Индексы: `messages(chat_id,id)`, `tokens(user_id)`, `group_members(group_id)`. **Нет:** таблицы версий, внешних ключей, индекса по `users.phone` (есть через UNIQUE), таблицы медиа, таблицы получателей/квитанций, поля `client_msg_id`, поля ревизии для синхронизации. Фактическая БД в `go-server/deploy/mase.sqlite` (по состоянию на 29 июня): 0 пользователей и нет таблиц групп; `data/mase.sqlite` — схема legacy C++ (1 пользователь). **Реальных данных, которые надо сохранять, практически нет — это лучшее время ввести миграции.**

---

## 3. Риски (только подтверждённые в коде)

| № | Риск | Где | Чем грозит | Как устранить (фаза) |
|---|---|---|---|---|
| R-1 | Пароли: SHA-256 без соли и без стоимости | `auth.go:13-16`, сравнение `handler.go:146` | утечка БД = мгновенный подбор большинства паролей | argon2id (или bcrypt cost ≥ 12), формат хеша с префиксом алгоритма; при успешном входе старый SHA-256 проверяется и **прозрачно перехешируется**; сравнение `subtle.ConstantTimeCompare` (Фаза 6) |
| R-2 | `CheckOrigin` всегда `true` | `main.go:19` | любая веб-страница может открыть сокет от имени залогиненного браузера (актуально при появлении веб-клиента) | разрешать пустой `Origin` (OkHttp его не шлёт) и allow-list; иначе 403 (Фаза 6) |
| R-3 | Нет rate limiting; IP клиента недоступен | весь `handler.go`; `main.go:48` логирует `RemoteAddr` (адрес туннеля) | перебор паролей, спам регистраций, заполнение диска | лимиты по IP (из `CF-Connecting-IP` / `X-Forwarded-For` только от доверенного прокси), по телефону и по пользователю; лимиты на регистрацию и число соединений (Фаза 6) |
| R-4 | Токены бессрочные, не отзываются, хранятся открытым текстом; токен в URL | `db.go:38-42,194-200`; `media.go:129-136`; клиент шлёт токен в каждом кадре | утерянный телефон/утечка БД = вечный доступ; токены попадают в логи прокси | таблица `sessions` (хеш токена, `expires_at`, `device`), скользящий срок 90 дней, `auth.logout`, `auth.logout_all`, `auth.change_password`; токен только в заголовке (Фаза 6) |
| R-5 | Бэкапов нет | `go-server/deploy/mase.sqlite*` | смерть диска/ПК = потеря всех сообщений | Фаза 2 |
| R-6 | Валидация медиа: тип берётся из заголовка клиента, файл целиком читается в память, нет проверки magic bytes, нет квот; любой авторизованный читает любой `mediaId`; CORS `*` | `media.go:58,62,68,78-80,88,110-124,169` | подмена типа, исчерпание RAM (100 МБ × N параллельных), хранение чужого контента, неучтённый рост диска | потоковая запись во временный файл, определение типа по первым байтам, таблица `media` (владелец, размер, тип, привязка к сообщению, `ACL` по чату), квоты, чистка сирот, `nosniff` и `Content-Disposition` (Ф8 — фото, Ф11 — файлы) |
| R-7 | Единая точка отказа: один домашний ПК, один процесс, нет автоперезапуска | `run.sh:37,57`, `mase.service` не установлен | выключился свет/завис ПК = мессенджер недоступен; при требовании 99,5 % (≈ 3,6 ч/мес) один такой случай съедает большую часть бюджета простоя | VPS в РФ как основной хост (раздел 4.6), systemd с `Restart=always`, внешний монитор, готовый резерв на домашнем ПК с переключением ≤ 30 мин, runbook (Фаза 4) |
| R-8 | Нет миграций схемы | `db.go:26-97` | любое изменение таблиц = ручной `ALTER` на проде или потеря данных | goose с встроенными миграциями, baseline = текущая схема (Фаза 3) |
| R-9 | TLS терминируется у Cloudflare: провайдер видит открытый текст сообщений и токенов; плюс с июня 2025 на российских сетях трафик к Cloudflare режется | `mase-tunnel.yml`, `run.sh:57` (флаг `--protocol http2` уже выбран из-за блокировки QUIC) | приватность и доступность без VPN | убрать Cloudflare из пути данных, TLS терминировать на своём сервере; E2E-шифрование — за рамками 1.0 (Фаза 1, 4) |
| R-10 | Разработка против прод-БД и прод-адреса | `run.sh:10`; `build.gradle.kts:18-19` | тестовые сообщения и правки схемы попадают в прод | флейворы `dev/prod`, отдельная БД и порт, `.env` (Фаза 0) |
| R-11 | Правка и удаление: ответ рассылается **всем онлайн-пользователям** без проверки автора; при чужом `messageId` БД не меняется, но событие уходит всё равно | `handler.go:509-541`, `db.go:315-325` | новый текст **личного** сообщения раскрывается всем онлайн; любой может подделать «удалено/изменено» для любого сообщения на клиентах, которые начнут обрабатывать событие | адресная рассылка только участникам чата, проверка `RowsAffected` и членства (Фаза 6) |
| R-12 | «Печатает…» рассылается всем | `handler.go:545-556` | утечка факта переписки | рассылать участникам `chatId` (Фаза 6) |
| R-13 | Квитанции: любой пользователь может пометить любое сообщение прочитанным; событие всем | `handler.go:491-505` | подделка статусов, утечка активности | проверять, что пользователь — получатель; per-recipient таблица (Фаза 7) |
| R-14 | Группы: любой участник добавляет людей; `leave` не проверяет членство и рассылает `member_left`; добавляемый пользователь не проверяется на существование; роли не используются | `handler.go:685-727` | подделка «участник вышел», захват групп | базовые проверки (Ф8), матрица ролей (Ф13) |
| R-15 | Падение всего процесса: `close(c.send)` в `Unregister` при том, что `Send`/`SendTo`/`Broadcast` пишут в канал уже после снятия блокировки | `hub.go:57` против `hub.go:85,95,113` | «send on closed channel» — паника в горутине без `recover` завершает сервер | закрывать канал только владельцем писателя, `recover` в обработчиках, тест с `-race` (Фаза 6). Скорее всего проявится при гонке отключения и рассылки; воспроизведение не пробовал |
| R-16 | Нет таймаутов HTTP-сервера, нет graceful shutdown, неаутентифицированные сокеты живут бесконечно | `main.go:67` | slowloris, зависшие соединения | `http.Server{ReadHeaderTimeout,...}`, дедлайн на аутентификацию 10 с, лимит соединений с IP (Фаза 6) |
| R-17 | Личные данные в логах | `handler.go:142,147,152,197` (телефон), `push.go:17` (начало push-токена) | утечка PII через логи | структурные логи, идентификатор пользователя вместо телефона (Фаза 4) |
| R-18 | Клиентский токен в резервных копиях, релиз без R8 и без подписи | `AndroidManifest.xml:13`, `build.gradle.kts:25` | утечка сессии, неподписываемая сборка, легко декомпилируется | `allowBackup=false`, `EncryptedSharedPreferences`/Keystore, R8, подпись (Фаза 5) |
| R-19 | Потеря сообщений при обрывах (нет очереди, нет ACK, нет досинхронизации) | см. 2.1 | «отправил — не дошло», «пропустил сообщение» | Фаза 7 |
| R-20 | Ключ подписи APK отсутствует, а после первой раздачи APK потеря ключа = невозможность обновить приложение у друзей | — | приложение придётся удалять и ставить заново | генерация и **два зашифрованных бэкапа в разных местах до первой раздачи** (Фаза 5) |
| R-21 | **Домашний белый IP** (для варианта «домашний ПК» и для резерва): домашний IP становится публично известен через DNS-запись; домашние тарифы часто запрещают размещение серверов и/или блокируют входящие 80/443; без Cloudflare пропадает защита от сканирования и мусорного трафика | не в коде — инфраструктура; проверяется в Ф1 (`docs/reachability.md`) | сканирование портов и DDoS/мусорный трафик на домашний адрес, раскрытие домашнего IP, отключение услуги или блокировка портов провайдером, невозможность выпустить сертификат (нужен порт 80 для HTTP-01) | основной хост — VPS в РФ (его IP публичен по определению и не домашний); домашний ПК только как резерв и только на время инцидента; в Ф1 проверить, открыты ли входящие 443 и 80, и прочитать договор о размещении серверов; на любом хосте закрыть всё, кроме 80/443 и SSH по ключам, включить rate limits (Ф6) и `ufw` (Ф4) |

---

## 4. Исследование хостинга (данные на сентябрь 2026)

> ⚠️ **Данные этого раздела устаревают за месяцы** (лимиты бесплатных тарифов, блокировки и замедления, цены, правила Google). **Перепроверить перед принятием D1 и ещё раз перед релизом 1.0** (Ф17); список и сроки — в 4.7.

### 4.1 Важнейшее ограничение: доступность из России

- С **9 июня 2025** российские провайдеры (Ростелеком, МегаФон, Вымпелком, МТС, МГТС) ограничивают соединения к сервисам за Cloudflare: после ~16 КБ передачи соединение фактически обрывается; Cloudflare заявляет, что не может это исправить со своей стороны. Публикации 2025–2026 указывают, что ограничение продолжается ([Cloudflare](https://blog.cloudflare.com/russian-internet-users-are-unable-to-access-the-open-internet/), [Медиазона](https://en.zona.media/article/2025/06/19/cloudflare), [BleepingComputer](https://www.bleepingcomputer.com/news/technology/russias-throttling-of-cloudflare-makes-sites-inaccessible/)). По тем же сообщениям под ограничения попали и зарубежные хостеры вроде Hetzner и DigitalOcean; для каждого конкретного провайдера я это **не проверял**.
- Долгоживущий WebSocket и загрузка фото — как раз тот трафик, который упирается в такой лимит.
- Мобильный интернет в России регулярно отключается, во время отключений работает «белый список» (с сентября 2025 — сначала 57 сайтов, включая госмессенджер Max); список расширяется ([Медиазона, 2026](https://en.zona.media/article/2026/04/07/russian_internet_censorship_2026)). Публикация, которую я нашла про хостеров в белом списке (Beget, RuVDS, EuroByte и др.), — коммерческий агрегатор ([beget-free.ru](https://beget-free.ru/kakie-xostingi-v-belom-spiske/)); **считать непроверенной**.
- В мае–июне 2026 были массовые сбои у хостеров (изъятие серверов в Нидерландах, обновления ТСПУ у российских хостеров) — не хранить всё у одного провайдера ([Amnezia](https://amnezia.org/ru/blog/cascading-hosting-issues-may-june-2026)).
- Следствие: **любой вариант «зарубежный бесплатный хостинг/CDN» рискует не удовлетворить требованию «без VPN»**, даже если технически бесплатен. Проверка измерением — Фаза 1, до любого переезда.

### 4.2 Хост для Go-сервера (долгие WebSocket, без засыпания)

| Вариант | Бесплатно | Нужна карта | Долгие WS без засыпания | Диск | Из РФ без VPN | Что при превышении / изменении условий |
|---|---|---|---|---|---|---|
| **Oracle Cloud Always Free** ([документация](https://docs.oracle.com/en-us/iaas/Content/FreeTier/freetier_topic-Always_Free_Resources.htm), [InfoQ](https://www.infoq.com/news/2026/07/oracle-cloud-free-tier-limits/)) | да | да + номер телефона | да, но простаивающие экземпляры (нагрузка < 20 % за 7 дней) Oracle может отобрать | блочный том | **регистрация недоступна:** Oracle ушла из России и Белоруссии ([Oracle](https://www.oracle.com/corporate/conflict-in-ukraine/russia/)) | с 15.06.2026 Ampere A1 урезан с 4 OCPU/24 ГБ до 2 OCPU/12 ГБ без объявления |
| **Fly.io** ([цены](https://fly.io/docs/about/pricing/), [триал](https://fly.io/docs/about/free-trial/)) | нет для новых аккаунтов (триал 2 VM-часа или 7 дней) | да | да | тома платные ($0,08/ГБ) | не проверялось | от ~5 $/мес |
| **Koyeb** ([scale-to-zero](https://www.koyeb.com/docs/run-and-scale/scale-to-zero), [инстансы](https://www.koyeb.com/docs/reference/instances)) | да, 1 инстанс 512 МБ / 0,1 vCPU | **да, даже для free** | **нет:** уходит в сон через 1 ч без HTTP-трафика, удерживаемый WebSocket сну не мешает | **нет томов** | не проверялось | автозасыпание нельзя отключить |
| **Render** ([free](https://render.com/docs/free), [WS-изменение](https://render.com/changelog/free-web-services-now-remain-active-while-receiving-websocket-messages)) | да, 750 ч/мес | не подтверждено | частично: с 24.02.2026 засыпает через 15 мин без входящего HTTP-запроса **или входящего WS-сообщения** (клиентский keepalive можно слать сообщениями); пробуждение ≈ 1 мин | **эфемерный, тома недоступны** | не проверялось | локальные файлы теряются при любом рестарте → SQLite невозможна |
| **Railway** ([планы](https://docs.railway.com/pricing/plans), [триал](https://docs.railway.com/pricing/free-trial)) | нет: триал 5 $ на 30 дней без карты, затем 1 $/мес кредита; Hobby 5 $/мес | нет для триала | да | тома платные | не проверялось | после исчерпания кредита сервисы останавливаются |
| **Домашний ПК + Cloudflare Tunnel** (как сейчас) | да | нет | да | свой | **под угрозой** — трафик идёт через Cloudflare (см. 4.1) | зависит от домашнего питания и провайдера |
| **Домашний ПК, прямой доступ** (белый IPv4, без Cloudflare, свой TLS) | да | нет | да | свой | **вероятно да** (трафик внутри РФ), **измерить**; в «белый список» при отключениях мобильного интернета не попадёт | нужен публичный IP (у многих провайдеров это платная услуга; за CGNAT не работает) |
| **VPS в РФ** (Timeweb, Beget, RuVDS и т.п.) | **нет**, 200–500 ₽/мес (ваша оценка; точные тарифы и SLA проверить в оферте) | оплата российской картой | да | свой | вероятно да, **измерить** (Ф1) | самый предсказуемый вариант; при росте — тариф выше, тот же бинарник |
| **Yandex Cloud** (триал-грант) ([условия](https://cloud.yandex.com/en/docs/free-trial/concepts/usage-grant)) | грант ≥ 4000 ₽ на 60 дней для резидентов РФ, потом платно | привязка платёжного аккаунта | да (ВМ) | да | вероятно да | не «бесплатно навсегда» |

**Итог с учётом новой цели.** Бесплатность теперь желательное, а не обязательное условие. Варианта, который одновременно бесплатен без карты, доступен из РФ без VPN и держит долгие WebSocket, я по-прежнему **не нашла**. Но и «бесплатный» домашний ПК не бесплатен (электричество, публичный IP) и без резерва не даёт 99,5 % (см. 4.6). Для рабочего инструмента рекомендуется **платный российский VPS с домашним ПК в резерве** — это решение **D1**.

### 4.3 Постоянное хранилище / БД

| Вариант | Лимиты free | Карта | Из РФ | Комментарий |
|---|---|---|---|---|
| **SQLite на локальном диске + бэкапы** | ограничен диском | нет | — | ничего не меняем в коде; критично только хранить копии **вне** машины |
| [Turso](https://turso.tech/pricing) | 5 ГБ, 500 млн чтений и 10 млн записей/мес, 100 БД | нет | не проверялось; соединение сервера с зарубежным хостом тоже пройдёт через российские ТСПУ | потребует смены драйвера и переписывания запросов |
| [Neon](https://neon.com/docs/introduction/plans) (Postgres) | 0,5 ГБ на проект, 100 CU-часов | нет | не проверялось | серверлесс с холодным стартом; смена БД = переписывание SQL |
| [Supabase](https://automationatlas.io/answers/supabase-free-tier-limits-2026/) (сторонний обзор) | 500 МБ БД, 1 ГБ файлов, 5 ГБ трафика | нет | не проверялось | **проект замораживается после 7 дней неактивности**; при риске потери данных ([история](https://simplebackups.com/blog/supabase-free-tier-paused)) |
| Эфемерный диск (Render/Koyeb free) | — | — | — | **исключён**: перезапуск = потеря всех сообщений |

**Выбор:** остаёмся на **SQLite (WAL) на постоянном диске сервера**. Внешняя БД добавляет зависимость от сервиса за границей, которая при ограничениях может отвалиться, и не даёт выигрыша при 50 пользователях. Off-site копии — Фаза 2.

### 4.4 Хранилище медиа

| Вариант | Free | Карта | Из РФ | Комментарий |
|---|---|---|---|---|
| **Локальный диск сервера** | по месту (сейчас свободно ≈ 226 ГБ на ПК) | нет | как и сам сервер | проще всего, квоты считаем сами |
| [Cloudflare R2](https://freetier.co/directory/products/cloudflare-r2) (сторонний обзор) | 10 ГБ, 1 млн операций записи и 10 млн чтения/мес, **исходящий трафик бесплатен** | **не подтверждено** (платёжный метод, по-видимому, требуется) | **отдача идёт через Cloudflare → тот же лимит 16 КБ для российских пользователей** | для аудитории в РФ непригоден как основной |
| [Backblaze B2](https://www.backblaze.com/cloud-storage/pricing) | 10 ГБ, исходящий бесплатно до 3× объёма хранения | **нет** | не проверялось | подходит как **место для бэкапов**, не для раздачи |
| Supabase Storage | 1 ГБ | нет | не проверялось | мало |

**Выбор:** медиа хранятся на диске сервера; в B2 (или на втором устройстве) уходят только копии. R2 не используем. Диск VPS считать по квоте: 50 пользователей × 1 ГБ — верхняя граница, фактический объём смотреть по факту; ориентир — диск от 40 ГБ.

### 4.5 Выбор по умолчанию и план отхода

**По умолчанию (решение D1, на согласование) — для 0.9 и для 1.0:**

- **Сервер:** VPS в РФ (KVM, от 1 vCPU / 1–2 ГБ RAM / 40 ГБ SSD — оценка на 50 пользователей, уточнить по факту), Caddy с автоматическим TLS, systemd, **без Cloudflare в пути данных**.
- **Резерв:** домашний ПК как готовый к запуску резерв (тот же бинарник и восстановление из копий, автопроверка раз в неделю), DNS-запись с TTL 60 с; переключение ≤ 30 мин (Ф4).
- **БД:** SQLite (WAL) + копии на две независимые площадки (Ф2). **Медиа:** диск VPS + квоты.
- **Стоимость:** 200–500 ₽/мес (≈ 2 400–6 000 ₽/год), оплата картой РФ. Полностью бесплатного варианта нет (см. 4.2 и 4.6).
- **Домашний ПК как основной хост** допустим только для разработки и для пилота с осознанным риском простоя (R-7, R-21).

**Порядок отхода (доступность измеряется на реальном трафике — Фаза 1):**

| Триггер | Действие | Время |
|---|---|---|
| Сбой VPS-провайдера дольше 30 мин | переключить DNS на резерв (домашний ПК) по `ops/failover.md`, данные из последней копии | ≤ 30 мин |
| Ограничения РКН/ТСПУ затронули выбранного провайдера (случаи июня 2026) | новый VPS у другого провайдера, тот же бинарник + восстановление из копии | ≤ 2 ч |
| Замер (Ф1) показал, что путь до VPS режется | другой провайдер или сеть; если режутся все — пересмотреть цель «без VPN» | — |
| Умер диск или сервер | развернуть на любой машине из копии по `RUNBOOK.md` | ≤ 2 ч |
| Зарубежное хранилище копий недоступно | второй носитель по Syncthing/`rsync` на другое устройство | |
| Условия бесплатного тарифа B2 изменились | другой дешёвый S3 или второй диск | |
| Вырос тариф VPS | сменить тариф или провайдера — перенос по runbook | ≤ 2 ч |

**Проблема без решения, о которой нужно знать:** во время отключений мобильного интернета доступны только сайты из «белого списка»; ни домашний IP, ни, вероятно, случайный VPS в него не попадут. Пока это ограничение принимается и вынесено в известные ограничения 1.0.

### 4.6 Сравнение: домашний ПК (0 ₽, риск простоя) против российского VPS (200–500 ₽/мес)

Оговорка: расчёт электричества — мои оценки при тарифе 6 ₽/кВт·ч (подставить свой тариф и замер мощности из Ф1); диапазон цен VPS — ваша оценка; SLA и цены конкретных провайдеров я не проверяла.

| Статья | Домашний ПК | Российский VPS |
|---|---|---|
| Плата за хостинг | 0 ₽ | 200–500 ₽/мес |
| Электричество, 24/7 | мини-ПК/ноутбук ≈ 15 Вт → ≈ 11 кВт·ч → **≈ 65 ₽/мес**; настольный ≈ 75 Вт → ≈ 54 кВт·ч → **≈ 325 ₽/мес** | 0 ₽ (включено) |
| Публичный IP | нужен «белый» IPv4; у многих провайдеров платная услуга, за CGNAT не работает (цену выяснить в Ф1) | включён |
| ИБП для отключений питания | желателен; разовая покупка, цену не проверяла | не нужен |
| **Итого в месяц** | **≈ 65–325 ₽ + плата за IP** | **200–500 ₽** |
| **Итого в год** | ≈ 780–3 900 ₽ + IP (+ ИБП) | ≈ 2 400–6 000 ₽ |
| Доступность | зависит от домашнего питания, провайдера, перезагрузок и от того, что ПК не выключили; без резерва один сбой на 2 ч съедает ≈ 56 % месячного бюджета простоя | у провайдеров обычно заявлен SLA порядка 99,9 % (**проверить в оферте**), независимое питание и канал; но сбои у российских хостеров были (июнь 2026, [Amnezia](https://amnezia.org/ru/blog/cascading-hosting-issues-may-june-2026)) → нужен резерв |
| Защита от сканирования и мусорного трафика | нет, домашний IP публичен (R-21) | IP публичен, но не домашний; `ufw`, rate limits |
| Условия договора | часто запрещено размещение серверов, могут блокироваться входящие 80/443 (проверить в Ф1) | размещение сервисов — предмет услуги |
| Дополнительно | — | данные хранятся в РФ (для рабочей группы из РФ это упрощает вопросы хранения персональных данных; не юридическая консультация) |

Бюджет простоя за месяц (30 дней = 720 ч):

| Доступность | Допустимый простой |
|---|---|
| 99 % | 7,2 ч |
| **99,5 % (цель)** | **3,6 ч** |
| 99,9 % | 43 мин |

**Рекомендация.** Мессенджер используется для учёбы и работы, а не для болтовни, поэтому разница в 0–430 ₽/мес несоизмерима со стоимостью простоя группы. «0 ₽» у домашнего ПК условны (электричество и IP), а требования 99,5 % и «сбой ≤ 2 ч» без резерва он не гарантирует. Рекомендую: **основной хост — российский VPS, домашний ПК — резерв и dev-стенд с проверяемым переключением ≤ 30 мин**. Решение принимаешь ты (D1); окончательно — после замеров Фазы 1 (доступность VPS с трёх операторов и фактическая цена).

### 4.7 Что перепроверять и когда

| Что | Почему устаревает | Когда |
|---|---|---|
| Лимиты и условия free tier (Oracle, Fly.io, Koyeb, Render, Railway, Turso, Neon, Supabase, R2, B2) | меняются без объявления (Oracle урезал ARM 15.06.2026) | **перед принятием D1 и ещё раз перед релизом 1.0** |
| Замедления и блокировки Cloudflare и зарубежных хостеров, «белые списки» | меняются за месяцы; проверять **замером `netprobe`**, а не публикациями | перед D1 и перед 1.0 |
| Цены, SLA и условия выбранного VPS-провайдера, его новостная история | | перед D1 и перед 1.0 |
| Правила Google для установки APK, условия RuStore | 30.09.2026 и 2027 | перед 1.0 (Ф16, Ф17) |

Дата последней проверки: 2026-09-19 (исходное исследование). Следующая: перед D1 (конец Ф1), затем перед релизом 1.0 (Ф17).

---

## 5. Решения, которые принимаешь ты

| № | Решение | Моя рекомендация | Когда нужно |
|---|---|---|---|
| **D1** | Где хостить: VPS в РФ / домашний ПК без Cloudflare / оставить туннель; готов ли платить 200–500 ₽/мес | **VPS в РФ** (доступность — жёсткое требование, расчёт в 4.6), домашний ПК — резерв и dev; домашний как основной — только для пилота с осознанным риском. Cloudflare Tunnel оставлять, только если замеры Ф1 проходят у всех трёх операторов и принят R-9 | Конец Фазы 1 |
| **D2** | Идентификатор входа: телефон (без подтверждения) или username | Перейти на **username + пароль**, телефон убрать (SMS-подтверждение платное, а неподтверждённый номер даёт ложную «надёжность» и персональные данные без пользы) | Начало Фазы 3 |
| **D3** | Канал push | См. Фазу 9: **основной — foreground-сервис с постоянным WebSocket, запасной — RuStore Push, FCM — по желанию** | Начало Фазы 9 |
| **D4** | Регистрация: открытая или по инвайт-коду | **По инвайт-коду** (один код на друга): защищает бесплатный ресурс от спама, дёшево в реализации | Фаза 6 |
| **D5** | SQLite-драйвер: оставить mattn/go-sqlite3 (CGO) или перейти на modernc.org/sqlite (без CGO) | modernc: статическая сборка без gcc, простая кросс-компиляция и CI. Минус: меньше опыта эксплуатации, чуть медленнее; при 50 пользователях несущественно | Фаза 3 |
| **D6** | Места хранения копий и квоты медиа | Копии: B2 (10 ГБ, без карты) + второе устройство; квота **1 ГБ/пользователь** (рабочие файлы до 50 МБ), диск сервера от 40 ГБ | Фаза 2 (копии), Фаза 8 (квота) |
| **D7** | Каналы распространения APK | Основной — свой сервер (`/download`, `/app/…apk`), дополнительно RuStore после 1.0 | Фаза 8 (страница), Фаза 16 (автообновление) |
| **D8** | Что считать «рабочим временем» для требования «единичный сбой ≤ 2 ч» | 08:00–20:00 МСК, Пн–Сб (значение по умолчанию в карте); уточнить фактический график группы | Фаза 4 |

---

## 6. Общий порядок и зависимости

```
Ф0 Гигиена ─► Ф1 Достижимость + стоимость + D1 ─► Ф2 Бэкапы ─► Ф3 Миграции ─► Ф4 Хостинг (VPS) и эксплуатация
      └────────────────────────► Ф5 Тесты, CI, подпись ─────────────────────────┘
Ф3+Ф5 ─► Ф6 Безопасность ─► Ф7 Надёжная доставка ─► Ф8 Ядро 0.9 (базовые группы, профиль, фото, раздача APK)
Ф4 ─────────────────────────────────────────────────────┘                   │
                                                                             ▼
                                          ╔═══════════════════════════════════════════╗
                                          ║  ВЕХА 0.9 «рабочая группа» → пилот 14 дней ║
                                          ╚═══════════════════════════════════════════╝
      после 0.9 — порядок по спросу; рекомендуемый (сверху вниз):
      Ф9  Push (закрытое приложение)          ← Ф4, Ф5, Ф8
      Ф10 Сообщения: правка, ответ, пересылка, закрепление, реакции    ← Ф8
      Ф11 Файлы до 50 МБ, вкладка «Файлы»     ← Ф8
      Ф12 Поиск по истории (FTS5)             ← Ф3, Ф8 (лучше после Ф10)
      Ф13 Роли, ссылка-приглашение, упоминания ← Ф8 (упоминания-push ← Ф9)
      Ф14 Голосовые                            ← Ф11
      Ф15 Закрепление/mute/архив/черновики, тема ← Ф8 (mute ← Ф9)
      Ф16 Автообновления                       ← Ф5, Ф8
все ─► Ф17 Приёмка и релиз 1.0
```

**Почему именно так.**

1. Ф0 нужна первой: без git любая правка необратима, а прод и разработка делят одну БД.
2. Ф1 стоит второй: если требование «без VPN» не выполняется на текущем пути, дальше нельзя строить ничего на этом фундаменте, а измерение и сравнение стоимости занимают вечера, а не недели.
3. Бэкапы (Ф2) идут **до** миграций (Ф3) и любых правок схемы: сначала страховка, потом операции над данными.
4. Хостинг и эксплуатация (Ф4) — **до** разработки функций: доступность теперь жёсткое требование, а группа будет тестировать на итоговом сетевом пути.
5. Тесты и CI (Ф5) до безопасности и протокола: изменения в них крупные и ломают клиент — нужна сетка.
6. Безопасность (Ф6) до надёжной доставки (Ф7), потому что Ф7 меняет протокол, а менять его дважды дороже.
7. **Веха 0.9 стоит сразу после Ф8**, а не после всех функций: как только есть безопасный сервер, надёжная доставка, личные чаты, базовые группы, фото и профиль, группа может начать работать; всё остальное определяется её реальным спросом (`docs/feedback.md`), а не нашими догадками.
8. **Базовые группы — до 0.9, расширенное управление — после.** Создать, писать, добавить, выйти нужны для работы сразу; роли, переименование, удаление участников, ссылка-приглашение и упоминания (Ф13) без них живётся.
9. Push (Ф9) — первый после 0.9: это единственное, чего группа не сможет терпеть долго. Он зависит от подписи (Ф5): RuStore привязывает push к отпечатку подписи.
10. Ф17 — только приёмка; новых функций в ней нет.

**Как читать блок «Готово когда»:** это сценарий, который выполняется руками, без обращения к коду; каждый шаг имеет наблюдаемый результат.

### 6.1 Оценка общего срока

Объём фаз переведён в недели при ~10 ч/нед: `S` = 0,5–1, `M` = 1–2, `L` = 3–4 недели.

| Фаза | Объём | Недели |
|---|---|---|
| Ф0 Гигиена | S | 0,5–1 |
| Ф1 Достижимость, стоимость, D1 | M | 1–2 |
| Ф2 Бэкапы | S | 0,5–1 |
| Ф3 Миграции БД | M | 1–2 |
| Ф4 Хостинг и эксплуатация | M | 1–2 |
| Ф5 Тесты, CI, подпись | M | 1–2 |
| Ф6 Безопасность | L | 3–4 |
| Ф7 Надёжная доставка | L | 3–4 |
| Ф8 Ядро 0.9 | L | 3–4 |
| Приёмка 0.9 | S | 0,5–1 |
| **До 0.9** | | **14,5–23 нед ≈ 3,5–5,5 мес** |
| Ф9 Push | L | 3–4 |
| Ф10 Сообщения | L | 3–4 |
| Ф11 Файлы | M | 1–2 |
| Ф12 Поиск | M | 1–2 |
| Ф13 Расширенные группы | M | 1–2 |
| Ф14 Голосовые | M | 1–2 |
| Ф15 Список чатов, оформление | M | 1–2 |
| Ф16 Автообновления | M | 1–2 |
| Ф17 Приёмка 1.0 | S | 0,5–1 |
| **После 0.9 до 1.0** | | **12,5–21 нед ≈ 3–5 мес** |
| **Итого до 1.0** | | **27–44 нед ≈ 6–10 мес (≈ 270–440 ч)** |

**Оговорка.** Это ориентир, а не обязательство: оценки сделаны по объёму задач, без учёта перерывов, переделок и незнакомых зон (push на российских прошивках, замеры сетей, FTS5). Результат Ф1 может изменить Ф4 в обе стороны, а пилот 0.9 — переставить Ф9–Ф16. С запасом ≈ 30 % на перерывы и переделки разумно планировать **8–13 месяцев до 1.0**; веха 0.9 при этом остаётся достижимой раньше — через 3,5–5,5 месяцев (с запасом 4,5–7).

---

## Фаза 0 — Гигиена репозитория и окружений

**Цель:** проект под git, без секретов и бинарников в репозитории, с раздельными окружениями dev и prod.
**Объём:** S · **Зависимости:** нет

- [x] `git init` в `/home/danil/code/mase`, ветка `main` (2026-09-19; перед этим сделана полная копия `~/mase-backup-20260919`, сверена `diff -rq`)
- [x] `.gitignore` (правило для загрузок якорное: `/media/`, `/go-server/deploy/media/` — общий `**/media/` проглатывал исходные пакеты `go-server/internal/media/` и `.../messenger/media/`): `*.sqlite*`, `**/media/`, `go-server/deploy/mase-server`, `dist/`, `**/build/`, `.gradle/`, `local.properties`, `*.keystore`, `*.jks`, `.env*`, `__pycache__/`
- [x] (2026-09-19: БД → `~/mase-data/`, старая C++-БД → `~/mase-data/legacy/cpp-mase.sqlite`, APK → `~/mase-data/apk-archive/`, хеши до и после совпали; бинарь удалён; пять пустых файлов `android-client/{A,Compilation,Get,Run,Task}` удалены. **Внимание:** `run.sh` по умолчанию всё ещё ждёт БД в `go-server/deploy/mase.sqlite` — до задачи с `MASE_DB` его не запускать, иначе создастся пустая БД) Вынести из дерева репозитория `dist/*.apk` (≈100 МБ), `data/mase.sqlite` и `go-server/deploy/mase.sqlite*` (в `~/mase-data/`), бинарь `go-server/deploy/mase-server` пересобирается и не хранится
- [ ] Перенести `cpp-server/`, `mase-server-gui.py`, `server_manager.py`, `server-icon.svg`, `mase-server-gui.desktop` в `legacy/` и убрать ветку `server` из `build_apk.sh`
- [x] (первый коммит `eac0cf9`, 57 файлов; 2026-09-19 репозиторий `Dendy3CTD/messenger_mase`, ветка `main`, первый push выполнен владельцем, `origin/main` = `eac0cf9`, автор корректен) Первый коммит; приватный репозиторий на GitHub; `git ls-files | xargs du -ch | tail -1` показывает размер < 5 МБ
- [ ] Сервер: параметры из переменных окружения (`MASE_ADDR`, `MASE_DB`, `MASE_MEDIA`, `MASE_LOG_LEVEL`) с теми же флагами как запасными; убрать жёсткие пути `/home/danil/...` из `run.sh`
- [ ] Отдельная dev-БД: `run.sh dev` использует `~/mase-data/dev.sqlite` и порт 8081; `run.sh prod` требует явный `MASE_DB`, иначе не стартует
- [ ] Android: `productFlavors { dev { applicationIdSuffix ".dev"; WS_URL = ws://10.0.2.2:8081/ws }, prod { WS_URL = wss://… } }`, разное имя приложения, `network_security_config.xml` разрешает cleartext **только** для `dev`
- [ ] Заменить `README.md` на актуальный: архитектура Go + WebSocket, запуск dev, сборка APK, структура; удалить разделы про OTP/mDNS/порты 5555–5556; `docs/PROTOCOL.md` — таблицы из раздела 2.2
- [ ] Удалить `ServerDiscovery.kt` и `TcpMessengerClient.kt` (мёртвый код)

**Готово когда:**
1. `git status` чист, `git log` содержит начальный коммит, `git ls-files` не содержит `*.sqlite`, `*.apk`, бинарь сервера.
2. Запускаю `./run.sh dev`, ставлю `devDebug` APK на телефон рядом с прод-приложением (обе иконки на экране), регистрируюсь в dev — пользователь появляется только в `dev.sqlite`, а `mase.sqlite` не изменился (сравнение по `sha256sum`).
3. Свежий `git clone` в другую папку собирает сервер и debug APK по инструкциям README без правок.

---

## Фаза 1 — Достижимость из России, стоимость и решение по хостингу

**Цель:** измерить, доходит ли трафик до сервера с российских сетей без VPN, сравнить домашний ПК и российский VPS по фактической стоимости и доступности и принять решение D1.
**Объём:** M · **Зависимости:** Ф0 (желательно; можно параллельно)

- [ ] Написать `tools/netprobe` (Go, один файл): подключается к `wss://…/ws`, отправляет `ping`, шлёт эхо-сообщение размером 1 МБ, удерживает соединение 30 мин с пингом раз в 30 с, скачивает файл 5 МБ по HTTPS, печатает таблицу «шаг / успех / время»
- [ ] Запустить на телефоне-хотспоте или Android-приложении-терминале в **трёх мобильных сетях** (МТС, МегаФон, Билайн/Теле2) и **двух домашних провайдерах** без VPN; заполнить таблицу результатов в `docs/reachability.md` (дата, оператор, регион, шаги)
- [ ] Повторить тот же тест для текущего адреса через Cloudflare Tunnel и для тестового адреса **напрямую** (см. следующие пункты)
- [ ] **Проверить входящие 443 и 80 на домашнем адресе:** с внешней сети (мобильный интернет другого оператора) выполнить `nc -vz <IP> 443` и `nc -vz <IP> 80` и прогнать внешний сканер портов; записать в `docs/reachability.md`, какие порты открыты, закрыты или фильтруются (порт 80 нужен для выпуска сертификата по HTTP-01)
- [ ] **Прочитать договор/оферту домашнего провайдера:** разрешено ли размещение серверов и публичных сервисов, есть ли запрет или блокировка входящих портов, сколько стоит «белый» IPv4; выписать номера пунктов и цитаты в `docs/reachability.md`; проверить, не CGNAT ли (`curl ifconfig.me` против адреса на роутере)
- [ ] Поднять временный прямой доступ: порт 443 на роутере → ПК, Caddy с сертификатом, DNS-запись **без проксирования** Cloudflare; прогнать `netprobe` тем же образом (это заодно репетиция резерва из Ф4)
- [ ] **Взять пробный период или один месяц у двух российских VPS-провайдеров**, поднять на каждом тестовый сервер и прогнать `netprobe` с тех же трёх операторов; записать цену, SLA из оферты, расположение дата-центра и когда провайдер в последний раз попадал в новости о сбоях
- [ ] **Замерить мощность домашнего ПК** ваттметром (простой и под нагрузкой) и взять тариф из квитанции; заполнить таблицу стоимости раздела 4.6 фактическими цифрами
- [ ] **Поставить внешний монитор на текущий прод** (`/health` раз в минуту с двух точек): с этого дня копится реальная статистика доступности для сравнения с 99,5 %
- [ ] Принять решение D1 по таблице замеров и стоимости; записать в раздел 4.5 и в «Журнал изменений»; повторить проверку данных раздела 4 (см. 4.7) — **перед принятием D1**

**Готово когда:** в `docs/reachability.md` есть (а) таблица минимум по 3 операторам × 3 пути (через Cloudflare / напрямую на домашний адрес / пробный VPS) с результатом «1 МБ прошёл / 30 минут соединение живо»; (б) результат проверки входящих 443 и 80 и выписка из договора о размещении серверов; (в) заполненная таблица стоимости 4.6 с замером мощности; по ним я объявляю выбранный путь и записываю D1 в раздел 4.5.

---

## Фаза 2 — Бэкапы

**Цель:** данные пережили смерть машины, и я это проверил.
**Объём:** S · **Зависимости:** Ф0; решение D6

- [ ] Скрипт `ops/backup.sh`: `sqlite3 mase.sqlite ".backup '/tmp/mase-YYYYmmdd.sqlite'"` (корректно для WAL, а не `cp`), затем `PRAGMA integrity_check`, `gzip`, шифрование `age` публичным ключом
- [ ] Ключ `age` (приватная часть) хранится **вне** машины с сервером (менеджер паролей + распечатка/флешка); без него копию не открыть — записать это в `RUNBOOK.md`
- [ ] Выгрузка копий: `rclone` в Backblaze B2 (ключ приложения с правом только на один bucket) **и** на второе устройство (Syncthing/`rsync`); перед этим проверить, что выгрузка с домашней машины в B2 проходит из РФ (если нет — только второе устройство и/или российское хранилище)
- [ ] Медиа: `rclone sync` каталога `media/` (только новые файлы, дельта)
- [ ] Расписание: `systemd timer` раз в сутки; хранение 7 ежедневных + 4 еженедельных; после успешного запуска пинг/письмо о результате (не Telegram — он нестабилен в РФ; предпочесть почту через SMTP российского провайдера)
- [ ] `ops/restore-drill.sh`: скачивает последнюю копию, расшифровывает, разворачивает в **чужом** каталоге, запускает сервер на порту 8082 и сравнивает количество строк в `users`, `messages` с исходной БД
- [ ] `RUNBOOK.md`, раздел «Восстановление»: пошаговая инструкция «новая машина за ≤ 2 ч»

**Готово когда:**
1. Создаю тестового пользователя и 10 сообщений; жду или запускаю таймер вручную; в B2 и на втором устройстве появляется свежий зашифрованный файл.
2. Останавливаю прод, переименовываю рабочую БД, запускаю `restore-drill.sh` **на другой машине** (или в чистой VM/каталоге): сервер стартует на восстановленных данных, тестовый пользователь входит, 10 сообщений на месте, приватный ключ `age` достаю **только из менеджера паролей**.
3. Время шага 2 записано в `RUNBOOK.md` и ≤ 2 ч.

---

## Фаза 3 — Миграции схемы БД

**Цель:** любое изменение схемы делается версионированным, проверенным скриптом, а схема доводится до состояния, нужного для остальных фаз.
**Объём:** M · **Зависимости:** Ф2 (страховка перед операциями над данными); решения D2, D5

- [ ] Подключить **goose** (`github.com/pressly/goose/v3`, миграции `embed.FS`), команды `mase migrate up|down|status`, автоприменение при старте с блокировкой (одна миграция за раз)
- [ ] Решение D5: если выбран modernc — заменить драйвер, убрать `gcc` из `setup.sh`; `PRAGMA foreign_keys=ON`, `busy_timeout`, `journal_mode=WAL`, `synchronous=NORMAL` через DSN (`db.go:17`)
- [ ] Миграция `0001_baseline.sql` = **полностью текущая схема** (`db.go:26-97`), идемпотентная для существующих БД (`IF NOT EXISTS`) — проверить на копии `deploy/mase.sqlite` (без групп) и на пустой БД
- [ ] Миграции изменения схемы (каждая — отдельным файлом с `-- +goose Down`):
  - [ ] `users`: `password_algo`, `login` (по D2), `banned_at`, `deleted_at`, `last_seen_at`; убрать `''`-username (`UNIQUE` + `CHECK(username IS NULL OR length(username) >= 3)`)
  - [ ] `sessions` вместо `tokens`: `token_hash`, `user_id`, `device_id`, `created_at`, `last_used_at`, `expires_at`, `revoked_at`
  - [ ] `chats`: `created_at`, `rev`; `chat_members(chat_id, user_id, role, joined_at, left_at)` — единая модель для direct, group, global
  - [ ] `messages`: `client_msg_id TEXT`, `UNIQUE(sender_id, client_msg_id)`, `rev INTEGER` (монотонный счётчик изменений), `forward_from_*`, `updated_at`
  - [ ] `message_receipts(message_id, user_id, delivered_at, read_at)`
  - [ ] `media(id, owner_id, size, mime, name, sha256, created_at, attached_at, chat_id)`
  - [ ] `devices(id, user_id, provider, token, app_version, last_seen)` вместо `push_tokens`
  - [ ] `blocks(user_id, blocked_id, created_at)` и `reports(id, reporter_id, target_id, message_id, reason, created_at)`
  - [ ] индексы: `messages(chat_id, rev)`, `messages(rev)`, `chat_members(user_id)`, `media(owner_id)`
- [ ] Таблицы для функций поздних фаз (реакции, FTS, закрепление, ссылки-приглашения, mute) **не создаются заранее** — каждая приходит отдельной миграцией в своей фазе; это и проверка того, что процесс миграций работает
- [ ] Данные из старых таблиц переносятся в теле миграций; проверка на тестовой БД с 1000 сообщений: количество строк до и после совпадает
- [ ] Android: Room `exportSchema = true`, каталог `schemas/` в git, тест `MigrationTestHelper` для 3→4 (появятся поля в Фазе 7)

**Готово когда:**
1. Беру копию БД из Ф2, применяю `mase migrate up` — статус показывает все версии; `mase migrate down 1` откатывает последнюю без ошибок; `up` снова.
2. Запускаю сервер на пустой БД и на копии старой — оба поднимаются и проходят `sqlite3 … "PRAGMA foreign_key_check; PRAGMA integrity_check;"` с пустым результатом / `ok`.
3. На копии со старыми данными вход по старому паролю работает (хеш пока прежний).

---

## Фаза 4 — Целевой хостинг и эксплуатация

**Цель:** сервер работает как служба на выбранном по D1 хосте с доступностью ≥ 99,5 %, сам поднимается после сбоя, а на случай отказа хоста есть проверенный резерв.
**Объём:** M · **Зависимости:** Ф1 (решение D1), Ф2; решение D8

- [ ] Единый бинарь: `go build -trimpath -ldflags "-s -w -X main.version=…"`, версия отдаётся в `/healthz`
- [ ] **Провизия VPS** (если D1 = VPS): Debian/Ubuntu LTS, SSH только по ключам (`PasswordAuthentication no`), `ufw` открывает только 22, 80, 443, `fail2ban` для SSH, `unattended-upgrades` для обновлений безопасности
- [ ] systemd-юнит `mase.service` (свой пользователь `mase`, `StateDirectory=mase`, `Restart=always`, `RestartSec=3`, `NoNewPrivileges`, `ProtectSystem=strict`, `LimitNOFILE=4096`), актуальные пути; удалить устаревшие `setup.sh`/`nginx.conf` или привести к реальности
- [ ] Caddy: обратный прокси на `127.0.0.1:8080`, автоматический TLS, `/ws` без таймаута простоя, лимит тела `/upload`, заголовки безопасности, `Cache-Control: private, no-store` для `/media/` (сейчас `public, immutable` в `nginx.conf:54-55`)
- [ ] `/healthz` (живость) и `/readyz` (ответ БД за < 200 мс, свободного места на диске > 1 ГБ), реальные проверки вместо `ok` (`main.go:62-64`)
- [ ] Структурные логи `log/slog` в JSON: уровень, `req_id`, `user_id`; **нет** телефонов, паролей, токенов, тел сообщений; заменить `log.Printf` во всём `internal/`
- [ ] Graceful shutdown по SIGTERM: закрыть сокеты с кодом 1001, дописать буферы, закрыть БД
- [ ] `ops/deploy.sh`: заливка бинарника, атомарная подмена и `systemctl restart`; простой ≤ 5 с; выполняется вне рабочего времени (после 20:00 МСК) с предупреждением группе за день
- [ ] **Внешний монитор:** две независимые точки проверяют `/readyz` раз в минуту; письмо при падении на два адреса (SMTP российского провайдера; не Telegram — он нестабилен в РФ); **ежемесячный отчёт доступности** в `docs/uptime/ГГГГ-ММ.md` со сравнением с 99,5 % и списком сбоев дольше 2 ч в рабочее время
- [ ] **Резерв:** домашний ПК получает копии (Ф2) и раз в неделю автоматически проходит `restore-drill.sh`; сон и гибернация отключены, автостарт после сбоя питания включён в BIOS; `ops/failover.md` — пошаговое переключение DNS (TTL записи 60 с) и запуск сервиса на резерве, и обратно после инцидента
- [ ] Если по D1 выбран Cloudflare Tunnel — задокументировать в `RUNBOOK.md`, что это осознанный риск (R-9)
- [ ] `RUNBOOK.md`: «сервис упал», «диск заполнен», «сменился IP», «переключиться на резерв», «обновить сервер без потери соединений» (короткое окно перезапуска — клиент переподключится, Фаза 7)

**Готово когда:**
1. `sudo systemctl kill -s KILL mase` — через ≤ 10 с сервис снова `active (running)`, клиент переподключился сам.
2. Перезагружаю машину — сервер и TLS поднялись без входа в систему, `curl https://…/readyz` возвращает 200.
3. Останавливаю сервис на 6 минут — на почту приходит письмо о недоступности; запускаю — письмо о восстановлении.
4. `journalctl -u mase --since -1h | grep -E "password|token|\+?[0-9]{10}"` пусто.
5. **Учения по переключению:** останавливаю основной хост, по `ops/failover.md` переключаю DNS на резерв — через ≤ 30 мин клиенты работают на резерве, история на месте.
6. `ops/deploy.sh` выкатывает новую версию при подключённых клиентах: простой ≤ 5 с, дублей сообщений нет.
7. `ssh -o PasswordAuthentication=yes` на сервер отклоняется; сканирование снаружи показывает только порты 22, 80, 443.
8. Через неделю есть первый отчёт `docs/uptime/…` от внешнего монитора.

---

## Фаза 5 — Тесты, CI, сборки и подпись APK

**Цель:** каждое изменение проверяется автоматически, а релизная подпись готова и сохранена **до** первой раздачи APK.
**Объём:** M · **Зависимости:** Ф0

- [ ] Сервер: пакет `internal/testutil` с временной SQLite-БД и применением миграций; тесты `auth` (хеш, токены), `db` (пользователи, чаты, сообщения, группы, миграции), `handler` (через `httptest` + `gorilla` клиент: login, register, resume, отправка, история)
- [ ] Тесты, **фиксирующие найденные дефекты** (сначала красные, чинятся в Ф6–Ф10): чужой `edit/delete`, `typing` вне чата, `leave` не участником, `chat.history` группы, пустой `username` у второго пользователя
- [ ] `go test -race ./...` без падений; `go vet`, `staticcheck` в CI; покрытие ≥ 60 % для `auth`, `db`, `handler`
- [ ] Android: unit-тесты `MessengerEngine` (парсинг событий, очередь — после Ф7), тест миграции Room, минимум один UI-тест входа (Compose test)
- [ ] GitHub Actions: `server.yml` (тесты, race, vet), `android.yml` (`assembleDevDebug`, `testDevDebugUnitTest`, Android Lint), кеш Gradle/Go; статус на `main` обязателен
- [ ] **Keystore:** `keytool -genkeypair -v -keystore mase-release.jks -alias mase -keyalg RSA -keysize 4096 -validity 10000`, пароль в менеджере паролей
- [ ] **Резервирование ключа (до первой раздачи APK):** две зашифрованные копии `mase-release.jks` (`age`/`gpg`) в **разных** местах (флешка в сейфе/у родственника и облако), плюс записанный `sha256`-отпечаток сертификата (`keytool -list -v`) в `RUNBOOK.md`; проверка: расшифровать копию на другой машине и подписать тестовый APK
- [ ] `signingConfigs.release` читает путь/пароли из `~/.gradle/gradle.properties` или переменных окружения, ключ **не в git**; в CI релизный ключ хранится как секрет репозитория (base64) и используется только на теге `v*`
- [ ] `release`: `isMinifyEnabled = true`, `isShrinkResources = true`, правила R8 для Room/OkHttp/org.json; `allowBackup=false`; токен хранить в `EncryptedSharedPreferences`/Keystore вместо открытого DataStore
- [ ] `versionCode` вычисляется из числа тегов/`git rev-list --count`, `versionName` из тега

**Готово когда:**
1. Открываю PR с намеренно сломанным тестом — CI красный, слияние заблокировано; исправляю — зелёный.
2. `./gradlew assembleProdRelease` даёт подписанный APK: `apksigner verify --print-certs app-prod-release.apk` показывает мой сертификат, размер ≤ 15 МБ, `unzip -l` не содержит `debug`.
3. На **другой** машине достаю ключ из резервной копии, подписываю APK, `apksigner` показывает **тот же отпечаток**.

---

## Фаза 6 — Безопасность и авторизация сервера

**Цель:** закрыть подтверждённые дыры в аутентификации и авторизации, чтобы сервер можно было показывать людям.
**Объём:** L · **Зависимости:** Ф3, Ф5; решения D2, D4

- [ ] **Пароли:** `argon2id` (`golang.org/x/crypto/argon2`, параметры m=64 МБ, t=3, p=2) или bcrypt cost 12; хеш хранится как `$argon2id$…` с префиксом; вход по старому SHA-256: проверить, если верно — сразу перехешировать и сохранить (`password_algo`); `subtle.ConstantTimeCompare`; тест: старый хеш → вход → в БД новый формат; пароль ≥ 8 символов при регистрации
- [ ] **Сессии:** токен 32 байта, в БД только `sha256(token)`; срок 90 дней скользящий, `auth.logout`, `auth.logout_all`, `auth.change_password` (отзывает остальные сессии), `err: session_expired` понятен клиенту
- [ ] **Аутентификация на соединении:** первый кадр — `auth.resume`/`login`/`register`; неаутентифицированный сокет закрывается через 10 с; токен из каждого кадра убрать (Ф7 пишет протокол v2 полностью — здесь только серверная часть, совместимая со старым клиентом на переходный период)
- [ ] **Rate limiting** (`golang.org/x/time/rate`): вход и регистрация — 5/мин на IP и 10/час на логин; отправка — 20 сообщений/10 с на пользователя; до 5 одновременных соединений с одного IP; IP брать из `CF-Connecting-IP`/`X-Forwarded-For` **только** если запрос пришёл от доверенного прокси (настройка `MASE_TRUSTED_PROXIES`)
- [ ] **Инвайты (D4):** таблица `invites(code, created_by, used_by, expires_at)`, `auth.register` требует код; команда `mase invite create`
- [ ] **Валидация:** логин `^[a-z0-9_]{3,32}$`, `displayName` ≤ 64 символов, `bio` ≤ 200, `body` ≤ 4000 рун, название группы ≤ 64, участников при создании ≤ 200, `replyToId` принадлежит тому же чату; `username` не может быть пустой строкой
- [ ] **Origin:** `CheckOrigin` пропускает пустой `Origin`, остальное — по списку `MASE_ALLOWED_ORIGINS` (`main.go:19`)
- [ ] **Права:** `edit/delete` только автору (или админу группы), рассылка **только участникам чата**, ответ отправителю с ошибкой `forbidden` если `RowsAffected=0` (`handler.go:509-541`); `typing` — участникам чата (`handler.go:545-556`); `friends.add` проверяет существование пользователя; `chat.direct.*` не создаёт чат с несуществующим id
- [ ] **Стабильность:** `recover()` в `Handle` и рассылках; переписать `hub` (закрывает канал только писатель, `Send` не паникует), `-race` тест на конкурентные connect/disconnect/send; `http.Server` с `ReadHeaderTimeout: 10s`, `IdleTimeout`; graceful shutdown (если не сделано в Ф4)
- [ ] `push.go:17` — не паниковать на коротком токене
- [ ] Логи: убрать номера телефонов, заменить на `user_id`/хеш (если не сделано в Ф4)
- [ ] **Блокировка:** `user.block{userId}`, `user.unblock`, `user.blocked_list`; заблокированный получает `forbidden` при попытке написать в личку заблокировавшему и **не получает** его `online` и `lastSeenAt` (в `presence` и `user.get`, Ф8); заблокировавшему не приходит `typing` заблокированного; общие группы не затрагиваются; тесты на все три эффекта
- [ ] **Удаление аккаунта:** `account.delete{password}` — `users.deleted_at`, отзыв всех сессий и закрытие сокетов, имя → «Удалённый пользователь», очистка аватара и био, логин резервируется навсегда, выход из групп с системным сообщением (при `owner` — передача прав как в Ф8), медиа пользователя в очередь удаления через 30 дней, сообщения остаются; вход после удаления → `account_deleted`; политика зафиксирована в разделе 1
- [ ] **Модерация:** `user.report{userId, messageId?, reason}` (не чаще 5 в сутки) пишется в `reports`; команды администратора `mase ban <login>` / `mase unban` (ставит `banned_at`, отзывает сессии, закрывает сокеты, запрещает вход и повторную регистрацию логина), `mase reports list`, `mase invite list|revoke <код>` (кто и когда использовал код — на случай утечки)
- [ ] **Последний визит:** `users.last_seen_at` обновляется при отключении сокета и раз в минуту при активности
- [ ] Тесты из Ф5, ранее красные, теперь зелёные

**Готово когда:**
1. Регистрируюсь двумя аккаунтами A и B и третьим C. Из C вручную (через `netprobe` или `websocat`) шлю `chat.message.edit` на сообщение из чата A–B — получаю `forbidden`, а A и B никаких событий не получают.
2. 6 неверных паролей подряд с одного IP — на 6-й приходит `rate_limited`; через минуту вход снова возможен.
3. Открываю сокет и молчу — через 10 с соединение закрыто.
4. `sqlite3 mase.sqlite "select password_hash from users limit 1"` начинается с `$argon2id$`; токены в таблице `sessions` — хеши; после `auth.logout` старый токен даёт `session_expired`.
5. Тест `go test -race ./internal/hub` с 1000 подключений/отключений проходит без паники.
6. A блокирует B: B при попытке написать A получает `forbidden`, `online` и `lastSeenAt` A для B недоступны; A разблокирует — снова можно.
7. `account.delete` тестового C: вход невозможен (`account_deleted`), у собеседников профиль «Удалённый пользователь», история на месте, файлы C удаляются по истечении срока (для проверки срок временно сокращается параметром).
8. `mase ban <login>`: активная сессия забаненного разрывается за ≤ 5 с, вход отклонён; `mase invite list` показывает, кто использовал код.

---

## Фаза 7 — Надёжная доставка (протокол v2)

**Цель:** сообщение не теряется при обрыве связи, приходит ровно один раз и клиент догоняет всё пропущенное.
**Объём:** L · **Зависимости:** Ф3, Ф6

**Дизайн (принят в карте, чтобы задачи не додумывались):**

- Клиент сам придумывает `clientMsgId` (UUID) для каждого исходящего; сервер дедуплицирует по `UNIQUE(sender_id, client_msg_id)` и отвечает `ack{clientMsgId, id, ts, rev}` — доставка «как минимум один раз» + идемпотентность = в UI ровно один раз.
- Сервер отправляет **полное состояние сообщения** с монотонной ревизией `rev` (создание, правка, удаление — один и тот же объект), клиент делает `upsert`, если `rev` больше. Так не бывает «событие пришло раньше, чем сообщение» (Ф10).
- Досинхронизация — по курсору `rev`: `sync.pull{afterRev, limit}` возвращает изменения всех чатов пользователя (включая новые чаты и членство) страницами до `hasMore=false`.
- Единая команда отправки `msg.send{chatId, clientMsgId, type, body, mediaId, replyToId, forwardOf}` заменяет 6 разных `chat.*.send`.
- Первый кадр — `hello{proto:2, appVersion}`; сервер отвечает `upgrade_required`, если версия ниже `minSupported` (используется в Ф16).

Задачи:

- [ ] Сервер: `msg.send`, `ack`, `sync.pull`, `chat.list`, `history{chatId, before, limit}` (страницы по 50); счётчик `rev` (таблица `counters`, увеличивается в транзакции)
- [ ] Сервер: получатель офлайн — ничего не теряется, сообщение лежит в БД и отдаётся `sync.pull`; получатель с переполненным буфером — соединение закрывается (клиент переподключится и досинхронизируется) вместо тихой потери (`hub.go:84-90`)
- [ ] Сервер: квитанции в `message_receipts`, состояния `sent → delivered → read`; `delivered` выставляется, когда клиент подтвердил получение (`ack.delivered`), `read` — когда чат открыт; в группах — счётчик прочитавших; события шлются только отправителю
- [ ] Сервер: `presence` — массив **объектов** `{userId}` или явный формат, исправить рассогласование (`handler.go:815-823` ↔ `MessengerEngine.kt:315-318`); `typing` — `isTyping` булево
- [ ] Сервер: `chat.list` возвращает по каждому чату `unreadCount` (по `message_receipts.read_at`), клиент больше не считает непрочитанные сам
- [ ] Сервер: правило «**одно соединение на пользователя**» (`hub.go:64-69`) сохраняется; при вытеснении старому соединению шлётся `session_replaced` и лишь затем оно закрывается; клиент по `session_replaced` показывает «Вход выполнен на другом устройстве» и **не переподключается сам**, пока пользователь не нажмёт «Войти здесь» (иначе два телефона бесконечно вытесняют друг друга)
- [ ] `tools/loadtest`: 10 клиентов в одной группе, каждый шлёт 1 сообщение/с в течение 10 минут; замеряются задержка доставки и потери (NFR: p95 ≤ 1 с, потерь нет)
- [ ] Клиент: **Room v4** — сообщение хранится с `localId` (PK), `clientMsgId`, `serverId?`, `rev`, `state` (`PENDING/SENT/DELIVERED/READ/FAILED`), поля `editedAt`, `isDeleted`, `replyToId`, `durationSec`, `senderName`; миграция 3→4 с тестом
- [ ] Клиент: **outbox** — сообщение сначала пишется в Room со статусом `PENDING` (появляется в UI сразу, с часами), затем отправляется; на `ack` заменяется серверным id; без `ack` за 15 с остаётся в очереди; при `FAILED` — кнопка «повторить»
- [ ] Клиент: **реконнект** — единый автомат состояний (`Disconnected → Connecting → Authenticating → Syncing → Online`); один сокет (предыдущий закрывается); экспоненциальная задержка 1→2→4…→60 с с джиттером ±20 %, **сброс после успешной аутентификации** (исправляет `MessengerEngine.kt:155-178`); мгновенная попытка при появлении сети (`ConnectivityManager.NetworkCallback`); первый кадр `auth.resume`, затем `sync.pull` с сохранённым курсором
- [ ] Клиент: обработчик всех кодов `err` (понятные тексты), обработка `rate_limited`, `forbidden`, `session_expired` (тихий выход на экран входа)
- [ ] Клиент: `WsMessengerClient.send` возвращает `Boolean`, вызывающий кладёт в outbox при `false` (`WsMessengerClient.kt:78-81`)
- [ ] Клиент: пагинация истории — подгрузка вверх по 50 при прокрутке; первая загрузка из Room без сети
- [ ] Клиент: список чатов строится из `chat.list` и `sync.pull`, а не только из входящих сообщений (новое устройство видит все чаты)
- [ ] Клиент: галочки — `⏳ / ✓ / ✓✓ / ✓✓(синие)` по состояниям; онлайн и «печатает» реально работают
- [ ] Тесты: сервер — дедупликация, `sync.pull` пагинация, порядок ревизий; клиент — outbox при офлайне, реконнект (фейковый WebSocket)

**Готово когда:**
1. Включаю авиарежим на телефоне A, пишу 3 сообщения B — в чате они с часами; выключаю авиарежим — через ≤ 10 с у B ровно 3 сообщения без дублей, у A часы сменились на `✓`.
2. B (телефон 2) выключен на 10 минут; A и C пишут B в личку и в группу; включаю B — все сообщения из всех чатов подтягиваются без открытия чатов.
3. Перезапускаю сервер при открытом приложении — баннер «Нет соединения» показывается один раз, через ≤ 30 с онлайн, в логах сервера одно соединение на пользователя.
4. Вхожу на втором телефоне под тем же аккаунтом — первый отключается с сообщением «Вход выполнен на другом устройстве» и **сам не переподключается**; на втором список чатов и вся история на месте.
5. Быстро нажимаю «отправить» 5 раз при плохой сети (режим «медленный 3G» в эмуляторе) — сообщений 5, а не 6+.
6. `tools/loadtest` (10 участников, 10 минут): p95 доставки ≤ 1 с, потерь нет.

---

## Фаза 8 — Ядро 0.9: базовые группы, профиль, фото, раздача APK

**Цель:** собрать минимально достаточный рабочий инструмент для проектной группы: личные и групповые чаты, профиль, фото и раздачу APK по ссылке.
**Объём:** L · **Зависимости:** Ф4, Ф5, Ф6, Ф7 (бэкапы Ф2 уже работают)

**Группы в 0.9 — намеренно упрощённые.** Полная матрица ролей, переименование, удаление участников, описание, ссылка-приглашение и упоминания — Ф13.

| Действие в 0.9 | owner (создатель) | member |
|---|---|---|
| Писать текст и фото | ✅ | ✅ |
| Добавить участника | ✅ | ❌ |
| Выйти | ✅ (права переходят, см. ниже) | ✅ |

### Группы (базовые)

- [ ] Сервер: сообщения группы идут через `msg.send` (Ф7) с проверкой по `chat_members`; `chat.history` работает для групп (исправляет `handler.go:468-474` и `db.go:231-243`), `senderName` приходит во всех сообщениях группы, в том числе в истории (`handler.go:482`)
- [ ] Сервер: `group.create{title, memberIds}` — создатель становится `owner`, участники проверяются на существование и на то, что не заблокировали создателя (`user.block`, Ф6)
- [ ] Сервер: `group.add_member` только от `owner`; повторное добавление идемпотентно; `group.leave` только от участника (закрывает подделку `member_left`, `handler.go:710-727`)
- [ ] Сервер, выход `owner`: права автоматически получает участник с наименьшим `joined_at`; вышел последний — группа помечается удалённой, её медиа уходят в очередь очистки; покрыть тестами каждый из трёх случаев
- [ ] Сервер: системные сообщения («X добавил Y», «X вышел») идут в общую ленту через `rev`; новый участник видит историю только с момента вступления
- [ ] Клиент: `CreateGroupScreen` (название, мультивыбор из друзей, ≥ 1 участник) и `GroupInfoScreen` (список участников, добавить, выйти) — сейчас методы `MessengerEngine.kt:521-564` не вызываются ниоткуда
- [ ] Клиент: навигация `AppRoot.kt`: «Новая группа», открытие группы из списка с `isGroup=true` (сейчас `AppRoot.kt:71-78` его не передаёт), переход в инфо из шапки
- [ ] Клиент: в группе над чужим сообщением имя отправителя и цвет (по id); в списке превью «Имя: текст» и иконка группы
- [ ] Клиент: после `leave` чат удаляется из списка только по подтверждению сервера (сейчас `MessengerEngine.kt:555-559` удаляет локально сразу)
- [ ] Тесты: серверные проверки базовых прав (не-owner не добавляет, не-участник не выходит, выход owner передаёт права); Compose-тест создания группы

### Профиль и люди

- [ ] Сервер: `user.get{userId | username}` возвращает публичный профиль: имя, `@username`, био, аватар, `online`, `lastSeenAt` (для заблокированного — без `online` и `lastSeenAt`); `profile.update` возвращает `username_taken` и ошибки валидации из Ф6; `users.last_seen_at` обновляется при отключении (поле из Ф3)
- [ ] Клиент: экран «Мой профиль» — правка имени, `@username` (проверка занятости при вводе), био, аватара; экран уже частично есть (`SettingsScreen.kt:124`), нужно довести до валидации и показа ошибок
- [ ] Клиент: экран «Профиль пользователя» по нажатию на имя или аватар (в шапке чата, в сообщении группы, в списке участников, в результатах поиска): имя, `@username`, био, «был(а) в сети …», кнопки «Написать» и «Заблокировать»
- [ ] Клиент: поиск человека по username в «Контактах» (поле уже есть, `ContactsScreen.kt:137`, `MessengerEngine.kt:571-575`) — результат ведёт на экран профиля, а не сразу в друзья
- [ ] Клиент: «Заблокированные» в Настройках со списком и кнопкой «Разблокировать» (серверная часть `user.block/unblock/blocked_list` — Ф6)
- [ ] Клиент: Настройки → «Удалить аккаунт» — предупреждение о последствиях (политика в разделе 1) и подтверждение паролем (серверная часть `account.delete` — Ф6)

### Список чатов и лента

- [ ] Клиент: счётчик непрочитанных берётся с сервера (`unreadCount` из `chat.list`, Ф7), а не считается локально (`ChatsListScreen.kt:122-152`, `MessengerEngine.kt:274-299`); сбрасывается при открытии чата отправкой квитанций; сохраняется при переустановке и на втором телефоне
- [ ] Клиент: при открытии чата прокрутка к первому непрочитанному сообщению (разделитель «Новые сообщения»)
- [ ] Клиент: разделители по дням в ленте «Сегодня», «Вчера», «12 сентября» (сейчас в ленте только время, `ChatScreen.kt:347`); в списке чатов формат «14:05 / вчера / 12 сент»
- [ ] Клиент: статус собеседника в шапке «в сети» / «был(а) сегодня в 14:05» / «был(а) 12 сентября»

### Фото

- [ ] Клиент: Coil `ImageLoader` с OkHttp-интерцептором `Authorization: Bearer …` и дисковым кешем — **первым делом**, чтобы фото и аватарки перестали давать 401 (`ChatScreen.kt:430`, `AvatarView.kt:38`)
- [ ] Сервер: таблица `media` (Ф3); загрузка потоком во временный файл, без `io.ReadAll` (`media.go:78-80`); тип по **первым байтам** (`http.DetectContentType`), расширение и `Content-Type` из проверенного типа, а не из заголовка клиента (`media.go:62`); лимит фото 5 МБ, квота на пользователя (D6), `quota_exceeded`
- [ ] Сервер: раздача только участникам чата, к которому прикреплён файл (ACL); `X-Content-Type-Options: nosniff`, `Cache-Control: private`; убрать `?token=` из URL (`media.go:129-136`) и CORS `*` (`media.go:169`)
- [ ] Сервер: `msg.send` с `type=photo` проверяет, что `mediaId` принадлежит отправителю и ещё не прикреплён (закрывает подстановку произвольного `mediaId`, `handler.go:430-457`); фоновая чистка неприкреплённых загрузок старше 1 ч
- [ ] Клиент: выбор из галереи и съёмка, сжатие 1280 px / JPEG 85 (уже есть, `MediaUploader.kt:19-20,101`), превью в ленте, полноэкранный просмотр с жестами

### Раздача APK по ссылке

- [ ] Сервер: страница `GET /download` (одна HTML-страница): кнопка «Скачать APK», версия, `sha256`, три шага установки («разрешить установку из этого источника», «открыть файл», «войти по инвайт-коду») и блок «Чтобы сообщения приходили при закрытом приложении» с инструкцией отключить оптимизацию батареи (временная замена push, см. веху 0.9)
- [ ] Сервер: `GET /app/mase-<версия>.apk` (публично, с `Range`) и `GET /app/version.json` (`versionCode`, `versionName`, `sha256`, `size`) — в 0.9 файл нужен только странице, автообновление в Ф16
- [ ] `ops/publish-release.sh`: копирует подписанный APK на сервер, считает `sha256`, атомарно обновляет `version.json` и страницу

**Готово когда:**
1. С телефона A создаю группу «Тест» из B и C; у B и C она появляется сразу, у D (не участник) — нет; пишу от A, B, C — у всех верные имена отправителей.
2. B (member) не видит кнопки «добавить участника», а прямой запрос `group.add_member` от B через `netprobe` возвращает `forbidden`.
3. A (owner) выходит — B (следующий по `joined_at`) становится owner, в ленте системное сообщение; B и C выходят — группа исчезает из списков, в БД помечена удалённой.
4. Отправляю фото из A в личный чат и в группу — у B оно видно сразу, открывается на весь экран; аватарка, установленная в профиле, видна у собеседников; `GET /media/<id>` без заголовка → 401; загруженный как фото `.exe` → `bad_type`.
5. Нажимаю на имя B в чате — открывается его профиль; нахожу C по username в «Контактах»; блокирую C — C больше не может мне писать и не видит моего статуса; разблокирую в Настройках.
6. Список чатов: счётчик непрочитанных совпадает после переустановки приложения; при входе в чат прокручивается к первому непрочитанному; в ленте разделители «Сегодня/Вчера».
7. На чистом телефоне открываю `https://…/download`, скачиваю APK, ставлю, регистрируюсь по инвайту.

---

## 🏁 Веха 0.9 — «рабочая группа»

**Цель:** как можно раньше получить инструмент, которым проектная группа реально пользуется каждый день, и дальше делать функции по фактическому спросу, а не по предположениям.
**Что это:** релиз `v0.9.0` после Ф8 и приёмки ниже; далее пилот 14 дней.

### Что входит

| Требование к 0.9 | Где реализовано |
|---|---|
| Безопасный сервер | Ф6 (пароли, сессии, rate limit, права, блокировка, бан) |
| Надёжная доставка | Ф7 (очередь, ACK, досинхронизация, реконнект) |
| Личные чаты | Ф7 + Ф8 (профиль, поиск человека, счётчик непрочитанных) |
| Базовые группы: создать, писать, добавить, выйти | Ф8 |
| Фото | Ф8 |
| Профиль | Ф8 |
| Бэкапы с проверенным восстановлением | Ф2 |
| Хостинг с мониторингом и автоперезапуском | Ф4 |
| Раздача APK по ссылке | Ф8 (`/download`) |

### Чего в 0.9 нет и как группа обходится

| Чего нет | Как группа обходится временно | Когда появится |
|---|---|---|
| Push при закрытом приложении | Оставлять приложение в фоне и отключить для него оптимизацию батареи (инструкция на `/download`); сообщения **не теряются** — доставляются при открытии; срочное дублировать другим каналом. Это самое ощутимое ограничение — поэтому Ф9 первая после 0.9 | Ф9 |
| Правка, удаление, ответ, пересылка, закрепление сообщения, реакции | Ошибку исправлять новым сообщением, на реакции отвечать коротким «+»; важное («шапку» проекта) держать в общем документе вне mase | Ф10 |
| Файлы (документы) | Обмениваться файлами вне mase (облако, почта), в чат — ссылка; скриншоты работают как фото | Ф11 |
| Поиск по истории | Прокрутка; договорённости записывать в общий документ проекта. История на сервере сохраняется, поиск можно будет включить задним числом | Ф12 |
| Роли, переименование группы, удаление участников, описание, ссылка-приглашение, упоминания, «кто прочитал» | Участников добавляет только создатель; чтобы убрать участника — создать новую группу; упоминать `@имя` текстом | Ф13 |
| Голосовые сообщения | Писать текстом | Ф14 |
| Закрепление чатов, mute, архив, черновики | — | Ф15 |
| Автообновления | Открыть `/download` и поставить новую версию поверх: данные и вход сохраняются (подпись та же, Ф5) | Ф16 |
| Несколько устройств одновременно | Один телефон на аккаунт (вход на втором отключает первый) | после 1.0 |

### Приёмка 0.9 (пройти руками до раздачи группе)

| № | Шаг | Ожидается |
|---|---|---|
| 1 | На чистом телефоне открыть `/download` по мобильной сети РФ без VPN, скачать и установить APK | установка без подсказок из чата |
| 2 | Регистрация по инвайт-коду, профиль, аватар | аватар виден у другого |
| 3 | Личный чат: текст, фото | доставка ≤ 2 с, галочки корректны |
| 4 | Группа из 3 человек: создать, писать, добавить четвёртого, выйти создателю | права переходят, системные сообщения |
| 5 | Авиарежим на 1 мин, 3 сообщения, выключить | ровно 3 сообщения, без дублей, пропущенные входящие подтянуты |
| 6 | Остановить сервер на 1 мин | клиенты переподключились сами, дублей нет |
| 7 | Вход на втором телефоне | первый отключается с понятным сообщением, не переподключается сам; на втором вся история |
| 8 | Заблокировать пользователя, удалить тестовый аккаунт | политика из раздела 1 соблюдена |
| 9 | Восстановление из копии на другой машине (`restore-drill.sh`) | чаты на месте, ≤ 2 ч |
| 10 | Остановить сервис на 6 мин | письмо о падении и о восстановлении |
| 11 | Пункты 1–3 из сетей двух других операторов | проходят |
| 12 | `journalctl` за время тестов | нет паролей, токенов, телефонов |

### Задачи

- [ ] Пройти приёмку 0.9, записать результат в `docs/acceptance-0.9.md`; исправить найденное
- [ ] Тег `v0.9.0`, публикация на `/download`, выдать инвайт-коды группе (до 10 человек)
- [ ] `docs/feedback.md` — журнал запросов (дата, кто, что, как часто, «блокирует работу?») и группа «mase-feedback» внутри приложения
- [ ] Пилот 14 дней: ежедневная запись аптайма, инцидентов и подтверждённых потерь сообщений
- [ ] По итогам пилота пересмотреть порядок Ф9–Ф16 и записать решение в «Журнал изменений»

**Готово когда (выход из пилота):** аптайм за 14 дней ≥ 99,5 % (не более ≈ 1,7 ч простоя), ни одного единичного сбоя дольше 2 ч в рабочее время, **0** подтверждённых потерь сообщений, все участники группы установили приложение без личной помощи, порядок Ф9–Ф16 подтверждён или изменён по `docs/feedback.md`.

---

## Фазы после 0.9 (порядок по спросу; рекомендуемый — сверху вниз)

Фазы 9–16 друг от друга почти не зависят, их можно переставлять по итогам пилота. Обоснование рекомендуемого порядка: сначала то, чего группа не сможет терпеть дольше всего (**push**), затем то, что нужно для проектной работы (**сообщения → файлы → поиск**), затем расширенное управление группой, и только потом «удобство» (голос, оформление, автообновление — поднять раньше, если релизы пойдут чаще раза в неделю).

## Фаза 9 — Уведомления, когда приложение закрыто

**Цель:** новое сообщение доходит до телефона за секунды, даже если приложение закрыто, и это работает на российских устройствах.
**Объём:** L · **Зависимости:** Ф4, Ф5 (подпись), Ф7, Ф8; решение D3

### Обоснование выбора канала (D3)

| Канал | За | Против |
|---|---|---|
| **Foreground-сервис с постоянным WebSocket** | Ни от кого не зависит (ни от Google, ни от RuStore), работает на устройствах без Google-сервисов, мгновенная доставка, уже есть WebSocket и досинхронизация | Постоянное уведомление в шторке, расход батареи, производители (Xiaomi, Huawei, Samsung и др.) убивают сервисы без исключения из оптимизации; на Android 15 тип `dataSync` ограничен по времени работы (≈ 6 ч в сутки), нужен тип `specialUse` (проверить по документации Android на момент реализации) |
| **RuStore Push** ([документация](https://www.rustore.ru/help/en/sdk/push-notifications)) | Бесплатно, не требует Google; будит убитое приложение | На устройстве должен стоять RuStore, разрешён в фоне и в нём выполнен вход ([условия](https://www.rustore.ru/help/en/sdk/general-push-notifications/kotlin/6-4-0)); в консоли нужно зарегистрировать отпечаток подписи APK (поэтому зависимость от Ф5); распространяется через российский магазин |
| **FCM** | Стандарт, есть у большинства телефонов с Google-сервисами, минимум кода | Зависит от Google Play Services; вы описали нестабильность в РФ, я не нашла свежих подтверждённых публикаций про её масштабы — проверять на реальных устройствах; серверные вызовы уходят к зарубежным адресам |
| **UnifiedPush / ntfy** | Открытый стандарт, можно поднять свой сервер | Пользователь должен установить отдельное приложение-дистрибьютор — **неприемлемо** для сценария «скачай APK и пиши» |

**Рекомендация:** реализовать в таком порядке — (1) **foreground-сервис** как основной механизм, доступный всем без условий; (2) **RuStore Push** как «будильник» для случая, когда сервис убит системой; (3) FCM — необязательный третий слой, добавляется, если тестирование на телефонах группы покажет, что первых двух недостаточно. Push-сообщения должны быть **«будильниками без содержимого»** (`{type:"wake"}`): приложение просыпается, подключается по WebSocket и подтягивает `sync.pull` — так текст сообщений не уходит через сторонние серверы. Решение принимаешь ты — при выборе «только FCM» задачи 2–3 ниже заменяются на Firebase, остальное без изменений.

### Задачи

- [ ] Сервер: таблица `devices` (провайдер, токен, версия, `last_seen`), несколько устройств на пользователя; интерфейс `push.Provider` (`RuStore`, `FCM`, `Noop`), настройки из окружения; неудачные токены удаляются
- [ ] Сервер: push при **любом** новом сообщении для офлайн-получателя (личные, группы, фото; сейчас только личный текст, `handler.go:398-406`), склейка по чату (не чаще 1 раза в 5 с на чат), очередь с повтором и тайм-аутом внешних вызовов
- [ ] Клиент: разрешение `POST_NOTIFICATIONS` (Android 13+) с объяснением, каналы уведомлений (сообщения, служебное), группировка по чату, счётчик, значок
- [ ] Клиент: `ForegroundService` (`foregroundServiceType="specialUse"`, `FOREGROUND_SERVICE_SPECIAL_USE`), держит WebSocket, автозапуск после перезагрузки (`RECEIVE_BOOT_COMPLETED`) и после обновления приложения; настройка «Работать в фоне» в Настройках
- [ ] Клиент: запрос исключения из оптимизации батареи (`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) и инструкция по автозапуску для популярных прошивок (экран «Не приходят уведомления?»); ту же инструкцию убрать из страницы `/download`, когда push заработает
- [ ] Клиент: интеграция RuStore Push SDK, регистрация токена через `device.register_push`, обработка `wake`
- [ ] Клиент: уведомление не показывается, если открыт этот же чат; по нажатию открывается нужный чат (`PendingIntent`)
- [ ] Клиент: настройка «Скрывать текст в уведомлениях»
- [ ] Метрика на сервере: доля сообщений, доставленных менее чем за 5 с при закрытом приложении, по каналам (в лог)

**Готово когда** (не меньше **трёх реальных устройств**, включая одно Xiaomi/Huawei/Samsung и одно без Google-сервисов, если такое есть):
1. Закрываю приложение смахиванием из недавних, с другого телефона шлю сообщение — уведомление появляется на первом ≤ 10 с.
2. Перезагружаю телефон, не открывая приложение, шлю сообщение — уведомление приходит.
3. Принудительно останавливаю приложение в настройках Android — приходит push через RuStore; открытие уведомления открывает нужный чат и подтягивает пропущенное.
4. Телефон в режиме экономии батареи на ночь — утром сообщения не потеряны, все подтянулись.
5. Включаю «Скрывать текст» — в шторке «Новое сообщение», без текста.

---

## Фаза 10 — Сообщения: правка, удаление, ответ, пересылка, закрепление, реакции

**Цель:** привычные действия над сообщениями работают надёжно, в том числе когда «связанного» сообщения ещё нет локально; для проектной работы добавлены закрепление и реакции.
**Объём:** L · **Зависимости:** Ф7, Ф8

- [ ] Клиент: долгое нажатие на пузырь → меню: ответить, копировать, изменить (только свои), удалить, переслать, закрепить, реакция
- [ ] **Правка:** `msg.edit{id, body, clientMsgId}`; окно правки 48 ч (константа на сервере); в UI пометка «изменено»; при отсутствии права — `forbidden`
- [ ] **Удаление для всех:** сообщение остаётся строкой с `isDeleted=1`, тело и связанное медиа очищаются (медиа — в очередь удаления), в UI — «Сообщение удалено»; `history` отдаёт флаг `isDeleted` (исправляет `handler.go:837-858`)
- [ ] **Обработка события о сообщении, которого нет локально:** т.к. по протоколу v2 приходит полное состояние с `rev`, клиент `upsert`-ит его (создаёт строку); если правка пришла для `serverId`, которого нет — клиент запрашивает `msg.get{id}`; порядок событий не важен; покрыть тестом «сначала правка, потом создание»
- [ ] **Ответ:** `replyToId` отправляется и показывается цитатой (автор + начало текста); если исходного сообщения нет локально — заглушка «Сообщение недоступно» и запрос `msg.get`
- [ ] **Переход к оригиналу:** нажатие на цитату прокручивает к исходному сообщению и подсвечивает его; если оно выше загруженной части — подгружает историю до него (`history{around: id}`)
- [ ] **Пересылка:** `forwardOf{chatId, messageId, senderName}` хранится в сообщении, показывается «Переслано от …»; пересылка допускается только из чатов, где пользователь состоит; пересылка медиа не копирует файл — ссылается на тот же `mediaId` (проверка прав в Ф11)
- [ ] **Выделение нескольких сообщений:** долгое нажатие включает режим выбора с галочками и счётчиком в шапке; действия: переслать (порядок сохраняется), удалить (только свои или, в группе, если есть право — Ф13), копировать
- [ ] **Закрепление сообщения:** `chat.pin{chatId, messageId}` / `chat.unpin`; закреплено одно сообщение на чат (новое заменяет прежнее), в личном чате закрепляет любой участник, в группе в 0.9–1.0 — `owner` (админы — Ф13); в шапке чата полоса «Закреплено: текст», нажатие ведёт к сообщению; в ленте системное сообщение. *Приоритет: для проектной группы это способ держать «шапку» проекта (ссылки, договорённости) под рукой, поэтому вынесено в 1.0.*
- [ ] **Реакции:** миграция `message_reactions(message_id, user_id, emoji, created_at)`; `msg.react{messageId, emoji | null}`; фиксированный набор из 8 эмодзи (👍 ❤️ 😂 😮 😢 🙏 ✅ 👀), одна реакция на пользователя и сообщение; изменение реакции поднимает `rev` сообщения; в UI счётчики-«чипы» под пузырём, нажатие переключает свою реакцию, долгое — список поставивших
- [ ] Сервер: рассылка изменений через `rev` только участникам чата (закрывает R-11)
- [ ] Тесты: правка чужого — отказ; удаление в группе; правка вне окна; идемпотентность повторной правки и повторной реакции; закрепление не участником — отказ

**Готово когда:**
1. A шлёт сообщение, B его видит; A меняет текст — у B он обновляется и помечен «изменено»; A удаляет — у B «Сообщение удалено» (без перезапуска).
2. B выключает сеть; A правит сообщение дважды и удаляет; B включает сеть — видит итоговое состояние «удалено», без пустых пузырей.
3. B отвечает на сообщение A цитатой; у A цитата показывает автора и текст; нажатие прокручивает к оригиналу, в том числе если оригинал старше загруженных 50 сообщений.
4. Выделяю 3 сообщения и пересылаю в группу — приходят в том же порядке с подписью «Переслано от …».
5. Закрепляю сообщение — у всех участников появляется полоса в шапке; нажатие ведёт к сообщению; в группе не-owner не может закрепить (`forbidden`).
6. B ставит 👍, A видит чип «👍 1»; A ставит 👍 тоже — «👍 2»; A убирает — «👍 1».
7. Чужой `messageId`, подставленный вручную в `edit`, возвращает `forbidden`.

---

## Фаза 11 — Файлы для проектной работы

**Цель:** группа обменивается рабочими документами до 50 МБ с прогрессом, докачкой и отменой, и находит все файлы чата в одном месте.
**Объём:** M · **Зависимости:** Ф8 (базовая медиа-инфраструктура), Ф10 (пересылка)

**Сервер**

- [ ] Возобновляемая загрузка: `POST /media/init{size, mime, name}` → `id`, затем `PUT /media/{id}` с `Content-Range`; `HEAD /media/{id}` возвращает принятый объём; лимит файла **50 МБ**, квота **1 ГБ** на пользователя (D6), превышение → `quota_exceeded`
- [ ] Тип определяется по первым байтам для известных форматов (PDF, изображения, `ftyp`, `OggS`); документы неизвестного типа хранятся как `application/octet-stream` и отдаются с `Content-Disposition: attachment`
- [ ] `msg.send` с `type=file` проверяет владельца и «непривязанность» `mediaId`; в БД сохраняются имя, размер, mime
- [ ] `chat.files{chatId, before, limit, query?}` — список файлов чата (имя, размер, тип, отправитель, время), только для участников; фильтр по подстроке имени
- [ ] Чистка: фоновая задача раз в 10 мин удаляет неприкреплённые загрузки старше 1 ч, недокачанные файлы старше 24 ч и файлы удалённых сообщений; лог с числом освобождённых байт; тест с искусственным временем

**Клиент**

- [ ] Отправка файла: системный `OpenDocument`, пузырь с иконкой типа (PDF, документ, таблица, архив, изображение, прочее), именем и размером
- [ ] Загрузка с прогрессом (`RequestBody`-обёртка), **отмена** (`Call.cancel()` + удаление недокачанного на сервере), **докачка** после обрыва (`HEAD` → продолжить с принятого смещения); очередь на `WorkManager`, чтобы отправка переживала закрытие экрана
- [ ] Скачивание в `Downloads` через `MediaStore` с прогрессом и докачкой (`Range`); если локальная копия удалена — кнопка «Скачать снова»; открытие системным приложением
- [ ] Вкладка **«Файлы»** в информации о чате (личном и групповом): список всех файлов чата по дате, иконка типа, имя, размер, кто прислал, поиск по имени; нажатие — открыть/скачать, долгое — переслать
- [ ] Понятные ошибки: `quota_exceeded`, «файл больше 50 МБ», «нет сети» с кнопкой повтора

**Готово когда:**
1. В группе из 10 человек A отправляет PDF на 50 МБ — у остальных пузырь с иконкой и размером, файл скачивается и открывается.
2. Во время загрузки включаю авиарежим на 20 с, выключаю — загрузка продолжается с прежнего места (в логах `Content-Range` не с нуля), сообщение отправляется один раз.
3. Нажимаю «отмена» на середине загрузки — на сервере нет файла после чистки, сообщение не создано.
4. Файл 51 МБ отклоняется до начала загрузки; при достижении квоты очередной файл получает `quota_exceeded`.
5. Открываю «Файлы» в информации о чате — вижу все файлы в порядке времени; ввожу часть имени — список фильтруется.
6. Очищаю кеш приложения → «Скачать снова» скачивает тот же файл; `mediaId` чужого чата в `msg.send` → `forbidden`.

---

## Фаза 12 — Поиск по истории

**Цель:** по любому слову находятся сообщения и чаты, к которым у пользователя есть доступ, за ≤ 1 с. Для проектной группы это обязательная функция, а не «удобство»: решения и договорённости теряются в переписке.
**Объём:** M · **Зависимости:** Ф3, Ф8; желательно Ф10 (правка и удаление должны обновлять индекс)

**Сервер**

- [ ] Миграция: `CREATE VIRTUAL TABLE messages_fts USING fts5(body, content='messages', content_rowid='id', tokenize='unicode61 remove_diacritics 2')` и триггеры на вставку, правку и удаление (удалённые и изменённые сообщения не должны находиться по старому тексту); драйвер должен быть собран с FTS5 — у mattn для этого нужен тег сборки `sqlite_fts5`, у modernc FTS5 включён (**проверить при реализации**; это довод в пользу D5)
- [ ] Индексация уже существующих сообщений в той же миграции (`'rebuild'`), тест на 100 000 сообщений: индексация не блокирует старт дольше 60 с
- [ ] `search.messages{query, chatId?, before?, limit}` — только чаты, где пользователь участник (JOIN по `chat_members` с учётом `joined_at` для групп); ответ — сообщение и фрагмент с подсветкой (`snippet()`), сортировка по свежести; запрос экранируется (синтаксис FTS пользователю не доступен), лимит 30 на страницу, rate limit
- [ ] `search.chats{query}` — по названию группы, имени и `@username` собеседника среди чатов пользователя

**Клиент**

- [ ] Локальный поиск по уже загруженным сообщениям на Room FTS (`@Fts4(contentEntity = MessageEntity::class)`) — мгновенный результат офлайн; серверный результат догружается и дополняет
- [ ] Экран поиска из списка чатов: результаты в двух группах «Чаты» и «Сообщения», нажатие открывает чат **на найденном сообщении** (подсвечено)
- [ ] Поиск внутри чата: иконка в шапке, счётчик совпадений «3 из 12» и переходы вверх/вниз
- [ ] Русская морфология не поддерживается (нет стемминга): к последнему слову запроса автоматически добавляется префикс `*` («отчёт» найдёт «отчёта»); записать это ограничение в README

**Готово когда:**
1. В базе 10 000 сообщений; по слову из сообщения трёхмесячной давности результат появляется ≤ 1 с и нажатие открывает чат на нём.
2. Меняю сообщение на «согласовано» — поиск «согласовано» находит, поиск старого текста — нет; удаляю сообщение — оно не находится.
3. Пользователь D, не состоящий в группе, по слову из её переписки ничего не находит (проверка через `netprobe` и в UI).
4. Без сети локальный поиск работает по загруженной истории.

---

## Фаза 13 — Расширенное управление группами и упоминания

**Цель:** роли, управление составом и инструменты для проектной работы в группе: упоминания, «кто прочитал», ссылка-приглашение.
**Объём:** M · **Зависимости:** Ф8; для уведомлений об упоминаниях — Ф9

**Матрица прав (принята):**

| Действие | owner | admin | member |
|---|---|---|---|
| Писать | ✅ | ✅ | ✅ |
| Добавить участника | ✅ | ✅ | ❌ |
| Удалить участника | ✅ | ✅ (не owner/admin) | ❌ |
| Переименовать, сменить аватар и описание | ✅ | ✅ | ❌ |
| Создать/отозвать ссылку-приглашение | ✅ | ✅ | ❌ |
| Закрепить сообщение | ✅ | ✅ | ❌ |
| Назначить/снять админа | ✅ | ❌ | ❌ |
| Выйти | ✅ (см. Ф8) | ✅ | ✅ |
| Удалить чужое сообщение | ✅ | ✅ | ❌ |

- [ ] Сервер: роль `admin` в `chat_members`, проверки прав из матрицы во всех обработчиках; команды `group.rename`, `group.remove_member`, `group.set_role`, `group.set_avatar`, `group.set_description` (≤ 200 символов); закрывает остаток R-14
- [ ] Сервер: системные сообщения для всех этих действий («X удалил Y», «X назначил Z администратором»)
- [ ] Сервер: **ссылка-приглашение:** `group.invite_create` / `group.invite_revoke`, код в `group_invites(code, group_id, created_by, expires_at, revoked_at)` (срок по умолчанию 7 дней); `group.join{code}` добавляет зарегистрированного пользователя; ссылка `https://<домен>/join/<код>` открывает страницу, которая передаёт в приложение `mase://join?code=…`
- [ ] Сервер: **упоминания** `@username` — при `msg.send` тело разбирается по участникам чата, найденные идентификаторы сохраняются в `messages.mentions`; упомянутый получает отдельный push с высоким приоритетом (Ф9), в том числе если чат заглушён (Ф15)
- [ ] Сервер: `msg.readers{messageId}` — список прочитавших с временем (из `message_receipts`, Ф7), доступен участникам группы для сообщений не старше 7 дней
- [ ] Клиент: `GroupInfoScreen` расширяется: роли, добавить/удалить, назначить админа, переименовать, аватар, описание, ссылка-приглашение (копировать/поделиться/отозвать), «удалить группу» для owner
- [ ] Клиент: при вводе `@` — список участников с автодополнением; упоминание подсвечено в пузыре; в списке чатов значок `@` у чата с непрочитанным упоминанием; фильтр «упоминания» в чате
- [ ] Клиент: долгое нажатие на своё сообщение в группе → «Прочитали (N)» со списком и временем
- [ ] Клиент: приём ссылки `mase://join?code=` (intent-filter, рядом с `mase://invite`, `AndroidManifest.xml:25-32`)
- [ ] Тесты: серверная матрица прав (таблица); срок и отзыв ссылки; упоминание не участника игнорируется

**Готово когда:**
1. A (owner) назначает B админом; B добавляет D и переименовывает группу — у всех новое имя без перезапуска; C (member) пытается удалить участника — кнопки нет, а через `netprobe` команда даёт `forbidden`.
2. A создаёт ссылку-приглашение, отправляет E; E нажимает на неё на телефоне с приложением — оказывается в группе; A отзывает ссылку — повторное вступление F по ней отклоняется.
3. B пишет «@c, посмотри отчёт» — у C уведомление «Вас упомянули» (даже когда группа заглушена), в списке чатов значок `@`.
4. Нажимаю «Прочитали» на своём сообщении — вижу, кто из 9 остальных и когда прочитал.
5. Описание группы, заданное админом, видно всем в информации о группе.

---

## Фаза 14 — Голосовые сообщения

**Цель:** запись и воспроизведение голосовых с настраиваемой скоростью.
**Объём:** M · **Зависимости:** Ф11 (возобновляемая загрузка и очередь)

- [ ] Сервер: `msg.send` с `type=voice`, `durationSec` ≤ 300, размер ≤ 5 МБ; тип проверяется по первым байтам (`ftyp`/`OggS`); длительность хранится в `duration_sec` (колонка уже есть); владелец и «непривязанность» `mediaId` проверяются так же, как в Ф11
- [ ] Клиент: разрешение `RECORD_AUDIO`; запись `MediaRecorder` **AAC-LC в `.m4a`, 32 кбит/с, моно** (работает на minSdk 24; Opus через `MediaRecorder` — только с API 29); удержание кнопки — запись, свайп влево — отмена, отпускание — отправка; таймер записи
- [ ] Клиент: плеер на `Media3`: play/pause, полоса прогресса, скорость 1× / 1,5× / 2× (`PlaybackParameters`), автопауза при звонке и потере аудиофокуса, запоминание позиции при повороте экрана
- [ ] Клиент: отправка через общую очередь загрузок Ф11 (прогресс, отмена, докачка)

**Готово когда:**
1. Записываю голосовое на 30 с и отправляю в личный чат и в группу — у B оно проигрывается, длительность верная, скорость 2× работает.
2. Записываю и свайпом влево отменяю — ничего не отправлено и файла на сервере нет.
3. Во время воспроизведения поступает телефонный звонок — воспроизведение приостанавливается.
4. Голосовое дольше 5 минут не отправляется, запись прерывается на 5:00.

---

## Фаза 15 — Список чатов и оформление «как в Telegram»

**Цель:** список чатов и внешний вид удобны при большом числе чатов; настройки синхронизируются между переустановками.
**Объём:** M · **Зависимости:** Ф8; для mute — Ф9
**Приоритет:** после функций, без которых нельзя работать (Ф9–Ф12); это удобство, но при 10+ чатах уже заметное.

- [ ] Закрепление чатов: `chat_members.pinned_at`, до 5 чатов, хранится на сервере (переживает переустановку), закреплённые сверху; долгое нажатие → «Закрепить»
- [ ] Mute: `chat_members.muted_until` (1 ч / 8 ч / 1 неделя / навсегда); сервер не шлёт push для заглушённого чата (кроме `@`-упоминаний, Ф13); в списке значок 🔕 и серый счётчик
- [ ] Архив: `chat_members.archived_at`; раздел «Архив» вверху списка; архивный чат возвращается в основной список при новом сообщении, если не заглушён
- [ ] Черновики: Room-таблица `drafts(chatId, text, updatedAt)`, сохраняется при уходе с экрана и при убийстве процесса, восстанавливается при открытии чата, в списке «Черновик: …» красным; очищается после отправки
- [ ] Тема: режимы «Светлая / Тёмная / Как в системе», по умолчанию «Как в системе» (сейчас только флаг `darkTheme=false`, `SessionRepository.kt:39,86`, `SettingsScreen.kt:254`); проверить контраст обеих тем на основных экранах
- [ ] Размер шрифта: не добавляем отдельную настройку — Compose уважает системный масштаб; задача — проверить, что ключевые экраны не ломаются при масштабе шрифта 1,3 (скриншот-тест)

**Готово когда:**
1. Закрепляю 2 чата — они сверху; переустанавливаю приложение и вхожу — закрепление сохранилось.
2. Заглушаю чат на 1 час — сообщение приходит без уведомления, счётчик серый; упоминание `@я` уведомление даёт.
3. Пишу текст, не отправляю, выхожу из чата и убиваю приложение — при возвращении текст на месте, в списке виден «Черновик».
4. Переключаю системную тему — приложение следует за ней; выбираю «Тёмная» вручную — не следует.
5. Системный размер шрифта 130 % — все основные экраны читаемы без обрезания кнопок.

---

## Фаза 16 — Автообновления

**Цель:** новые версии приходят внутри приложения без Google Play и без ручного скачивания.
**Объём:** M · **Зависимости:** Ф5 (подпись), Ф8 (`/download`); решение D7

- [ ] Сервер: в `version.json` добавляются `minSupported` и `releaseNotes`; `hello{proto, appVersion}` (Ф7): если `appVersion < minSupported` → `upgrade_required`
- [ ] Клиент: `UpdateChecker` — запрос при старте и раз в сутки (`WorkManager`), сравнение `versionCode`, диалог «Доступна версия X» с заметками; кнопка «Проверить обновления» в Настройках
- [ ] Клиент: скачивание APK с прогрессом и докачкой, **проверка `sha256`** до установки, установка через `PackageInstaller`; разрешение `REQUEST_INSTALL_PACKAGES` и подсказка включить «Установка из этого источника» (экран настроек по `ACTION_MANAGE_UNKNOWN_APP_SOURCES`)
- [ ] Клиент: блокирующий экран «Требуется обновление» по `upgrade_required`
- [ ] Подпись: Android при обновлении сам проверит совпадение сертификата — тест «APK, подписанный другим ключом, не ставится»
- [ ] Канал через **RuStore** (D7): не блокирует релиз; задача — завести аккаунт разработчика и опубликовать
- [ ] **Проверка правил Google:** с 30.09.2026 регистрация разработчика требуется для установки на сертифицированных устройствах в Бразилии, Индонезии, Сингапуре и Таиланде; распространение на все сертифицированные устройства заявлено на 2027 год, для непроверенных разработчиков появится «расширенный» процесс установки с задержкой ([Android Authority](https://www.androidauthority.com/android-sideloading-changes-timeline-3679204/), [справка Google](https://support.google.com/android-developer-console/answer/16561738)). В 2027 году это может затронуть российских пользователей; **задача:** до релиза 1.0 перечитать актуальные условия и решить, регистрироваться ли как разработчик (это независимый от Google Play шаг)
- [ ] `minSupported` и `versionCode` в CI выставляются из тега

**Готово когда:**
1. Публикую 0.9.1, на телефоне при запуске появляется «Доступна 0.9.1» → «Обновить» → скачивание с прогрессом → установка поверх, **данные и вход сохранены**.
2. Подмениваю `sha256` в `version.json` — приложение отказывается ставить.
3. Поднимаю `minSupported` выше установленной версии — приложение показывает блокирующий экран и не подключается.
4. APK, подписанный отладочным ключом, поверх релизного не устанавливается.

---

## Фаза 17 — Приёмка и релиз 1.0

**Цель:** подтвердить целевой сценарий «скачал → зарегистрировался → работаю» вручную на реальных устройствах, подтвердить доступность 99,5 % и выпустить 1.0.0.
**Объём:** S · **Зависимости:** все предыдущие

Новых функций в этой фазе нет — только исправление найденных дефектов.

### Чек-лист ручной приёмки (два телефона, одна мобильная сеть РФ без VPN, второй — Wi-Fi; версия — релизный подписанный APK с `/download`)

| № | Шаг | Ожидается |
|---|---|---|
| 1 | Открыть `/download` на чистом телефоне (мобильный интернет, без VPN) | страница открылась, APK скачался целиком |
| 2 | Установить, зарегистрироваться по инвайт-коду | вход выполнен, экран профиля |
| 3 | Заполнить профиль, загрузить аватар | аватар виден у другого пользователя |
| 4 | Найти друга по username, написать текст | доставлено ✓✓ за ≤ 2 с |
| 5 | Создать группу из трёх человек, написать от каждого | имена и роли верны |
| 6 | Отправить фото | видно у получателя, открывается на весь экран |
| 7 | Отправить документ ≥ 10 МБ | прогресс, скачивается, открывается |
| 8 | Отправить голосовое 30 с, прослушать на 2× | проигрывается, скорость меняется |
| 9 | Ответить, переслать, изменить, удалить | все действия отражаются у собеседника |
| 10 | Закрыть приложение смахиванием, с другого телефона написать | уведомление ≤ 10 с, открывает чат |
| 11 | Включить авиарежим на 1 мин, написать 3 сообщения из-под авиарежима, выключить | всё отправлено ровно один раз, входящие пропущенные подтянуты |
| 12 | Перезагрузить телефон, не открывая приложение | уведомления продолжают приходить |
| 13 | Выйти из аккаунта и войти снова | чаты и история на месте, старая сессия отозвана |
| 14 | Войти на втором телефоне под тем же аккаунтом | **первый телефон отключается** с сообщением «Вход выполнен на другом устройстве» и сам не переподключается; **на втором вся история и список чатов на месте** (одновременная работа на двух устройствах не поддерживается: сервер держит одно соединение на пользователя, `hub.go:64-69`) |
| 15 | Опубликовать 1.0.1, обновить внутри приложения | данные сохранены, версия изменилась |
| 16 | На сервере: остановить процесс на 1 мин | клиенты переподключились сами, без дублей |
| 17 | На сервере: выполнить учебное восстановление из копии на другой машине | все чаты на месте, время ≤ 2 ч |
| 18 | Расшифровать резервную копию ключа подписи на другой машине и подписать APK | отпечаток совпадает |
| 19 | `journalctl` за время тестов | нет паролей, токенов, телефонов |
| 20 | Мобильная сеть двух других операторов | пп. 1–4 и 10 проходят |
| 21 | Поиск слова из сообщения месячной давности | найдено ≤ 1 с, нажатие открывает чат на нём |
| 22 | Закрепить сообщение в группе | у всех участников полоса в шапке |
| 23 | Поставить и убрать реакцию | у собеседников счётчик меняется |
| 24 | Заблокировать пользователя, затем разблокировать | заблокированный не пишет и не видит статус; после разблокировки — снова может |
| 25 | Удалить тестовый аккаунт | «Удалённый пользователь», сообщения на месте, вход невозможен |
| 26 | Группа из 10 человек пишет одновременно 5 минут | потерь нет, задержка доставки ≤ 1 с |
| 27 | Файл 50 МБ в группе, затем вкладка «Файлы», затем переустановка | файл в списке, скачивается снова |
| 28 | Упомянуть `@username` в заглушённой группе | у упомянутого отдельное уведомление, у остальных тишина |
| 29 | Отчёт внешнего монитора за последние 30 дней | доступность ≥ 99,5 %, ни одного единичного сбоя > 2 ч в рабочее время |

- [ ] Перепроверить данные раздела 4 (лимиты и условия бесплатных тарифов, блокировки и замедления в РФ, «белые списки», цены и условия выбранного VPS, правила Google для APK) и записать дату проверки в раздел 4.7; повторить `netprobe` (Ф1) с трёх операторов
- [ ] Пройти чек-лист полностью, отметить результаты в `docs/acceptance-1.0.md`
- [ ] Исправить дефекты, найденные при приёмке; перепройти упавшие пункты
- [ ] Тег `v1.0.0`, релизные заметки, публикация на `/download`
- [ ] Обновить `README.md` и `RUNBOOK.md`, зафиксировать известные ограничения (нет E2E, один телефон на аккаунт, нет доступа во время отключений мобильного интернета из-за «белых списков», русский поиск без морфологии)

**Готово когда:** все 29 пунктов чек-листа выполнены на реальных устройствах, результат записан, версия 1.0.0 доступна по ссылке.

---

## Ближайшие 5 шагов

С учётом того, что доступность стала жёстким требованием: сначала измеряем и страхуемся, потом строим.

1. [ ] `git init`, `.gitignore`, вынести БД, бинарь и `dist/` из дерева, разделить dev и prod (`run.sh dev`, флейвор `dev` с другим `applicationId` и адресом) — Фаза 0, ≈ полдня
2. [ ] Поставить **внешний монитор на текущий прод сегодня же** (пока `/health` статичен, хватит и его): с этого дня копится реальная статистика доступности, а не догадки — Фаза 1
3. [ ] Написать `tools/netprobe`, измерить прод через Cloudflare и **пробный российский VPS** с трёх мобильных сетей без VPN; проверить входящие 443/80 и договор с домашним провайдером; заполнить таблицу стоимости 4.6 — Фаза 1, затем принять D1
4. [ ] Ежедневная копия SQLite через `.backup` + шифрование + выгрузка на второе устройство и **проверенное восстановление** — Фаза 2
5. [ ] Сгенерировать release-keystore и сделать **две** зашифрованные копии в разных местах — до того, как кому-либо будет отдан APK (Фаза 5)

---

## Идеи после 1.0 (вне основной карты)

**Вынесено из 1.0 намеренно (с обоснованием):**

- **Свайп-действия по чатам** — дублируют долгое нажатие (закрепить, mute, архив уже в 1.0), это скорость, а не возможность; берём после 1.0, если группа попросит
- **Собственная настройка размера шрифта** — Android масштабирует текст системной настройкой; в 1.0 достаточно проверки вёрстки при масштабе 1,3 (Ф15)
- **Несколько устройств одновременно** — сейчас сервер держит одно соединение на пользователя (`hub.go:31-32,64-69`); многоустройственность меняет модель сессий, доставки и квитанций, окупается только при реальном спросе; до тех пор действует правило «вход на втором телефоне отключает первый»

**Остальное:**

- **E2E-шифрование** личных чатов (протокол Signal / MLS через libsignal): большая работа — ключи устройств, верификация, резервные копии, конфликтует с серверным поиском (Ф12); сейчас сервер видит открытый текст
- **Звонки** (WebRTC): голос 1-на-1; нужен TURN-сервер, а он упрётся в те же ограничения сетей
- **Веб-клиент / десктоп** (после этого `CheckOrigin` придётся настраивать точечно)
- Стикеры, GIF, каналы и боты
- iOS-клиент (потребует своего канала push и платной подписки Apple)
- Переход на Postgres при росте свыше нескольких сотен пользователей

---

## Журнал изменений карты

| Дата | Что изменилось |
|---|---|
| 2026-09-19 | Первая версия. Основана на чтении кода (`go-server/`, `android-client/`), а не на заметках. Найдены и включены как риски: отсутствие проверок прав в `edit/delete/typing/receipt/group.leave`, отсутствие досинхронизации и очереди отправки, сломанный реконнект, 401 на фото и аватарки, рассогласование форматов `presence/typing/status`. Хостинг исследован по состоянию на сентябрь 2026 г.; главный вывод — Cloudflare-путь создаёт риск для требования «без VPN», решение D1 принимается по замерам Фазы 1. Порядок фаз изменён относительно прежней карты из Obsidian: гигиена, замер достижимости, бэкапы и миграции стоят **до** любых новых функций. |
| 2026-09-19 | **Вторая редакция: смена цели и веха 0.9.** (1) Цель уточнена: рабочий инструмент проектной группы, замена Telegram; доступность 99,5 % — жёсткое требование, бесплатность — желательное (шапка, разделы 0, 1). (2) NFR: группа до 10 человек в одном чате, рабочие файлы до 50 МБ (квота 1 ГБ), поиск по истории обязателен, политика удаления аккаунта. (3) Раздел 4: добавлены сравнение стоимости и доступности «домашний ПК против российского VPS» (4.6), рекомендация по умолчанию изменена на российский VPS с домашним ПК как резервом (4.5, D1), таблица «что перепроверять и когда» (4.7). (4) Новый риск R-21 «Домашний белый IP»; в Ф1 добавлены проверка входящих 443/80, чтение договора провайдера, пробные VPS и замер мощности ПК. (5) Введена веха **0.9 «рабочая группа»** после новой Ф8 «Ядро 0.9»; фазы пересобраны: базовые группы, профиль, фото и раздача APK — до 0.9; роли и управление группами — Ф13; старая Ф10 «Медиа» разделена на фото (Ф8), файлы (Ф11) и голосовые (Ф14); push — Ф9; автообновления — Ф16; приёмка 1.0 — Ф17. (6) Добавлены функции: блокировка (`user.block`), удаление аккаунта, бан и жалобы (Ф6), профиль и просмотр чужого профиля, «был(а) в сети», разделители по дням (Ф8), закрепление сообщения, реакции, мультивыбор (Ф10), список файлов чата (Ф11), поиск FTS5 (Ф12), упоминания, ссылка-приглашение, «кто прочитал» (Ф13), закрепление чатов, mute, архив, черновики, тема (Ф15). (7) Пункт 14 приёмочного чек-листа переформулирован под одно соединение на пользователя; в Ф7 добавлено сообщение `session_replaced` и запрет автопереподключения вытесненного устройства. (8) Добавлена оценка срока (6.1): 6–10 месяцев при 10 ч/нед. (9) Обновлены «Порядок и зависимости», «Ближайшие 5 шагов», «Идеи после 1.0». |
| 2026-09-19 | **Ф0, старт.** Сделана копия проекта `~/mase-backup-20260919`; `git init -b main`; `.gitignore` (найдена и исправлена ошибка шаблона `**/media/`, игнорировавшего исходники); БД, C++-БД и 6 APK перенесены в `~/mase-data/` с проверкой хешей; удалены бинарь сервера и пять пустых файлов; первый коммит `eac0cf9` (57 файлов). Найдено: `run.sh` по умолчанию указывает на перенесённую БД (см. Ф0). |
| 2026-09-19 | **Ф0: репозиторий на GitHub.** Push `eac0cf9` выполнен владельцем: `github.com/Dendy3CTD/messenger_mase`, ветка `main`, автор корректен (проверено `git ls-remote`). Имя репозитория внесено в шапку; в шапке исправлена устаревшая строка «проект не под git». Прежнее имя `Dendy3CTD/mase` встречалось только в вытесненной старой заметке Obsidian, в этой карте его не было. |
